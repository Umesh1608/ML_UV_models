\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{soul}
\usepackage{url}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{caption}

% Reviewer comments shown in indented italics
\newenvironment{reviewer}{%
  \begin{quote}\itshape\color{black!70}}{%
  \end{quote}}

% Author responses shown plain
\newcommand{\response}[1]{\noindent\textbf{Response:} #1\par\medskip}

% Manuscript changes shown bulleted
\newenvironment{changes}{%
  \noindent\textbf{Changes to manuscript:}
  \begin{itemize}[leftmargin=2em,itemsep=2pt]}{%
  \end{itemize}\medskip}

\title{Response to Reviewers\\
\large Manuscript ID: ci-2026-009433\\
``When Do Simple Models Win? Machine Learning Architectures for UV Absorption Prediction''}
\author{Umesh Arampath, Bryant Pero, David Stewart, David Demirjian}
\date{\today}

\begin{document}
\maketitle

We thank the Associate Editor and both reviewers for their thoughtful and constructive feedback. The reviewers' comments have substantially improved the manuscript, particularly by sharpening the scope of our claims, strengthening the data preprocessing and evaluation protocol, and adding analyses that contextualize our findings.

In the responses below, each reviewer comment is reproduced verbatim in indented italics, followed by our response and a bulleted list of corresponding changes to the manuscript. All page and line numbers refer to the revised manuscript unless stated otherwise.


\clearpage

\section*{Reviewer 1}

\subsection*{Comment R1-1: Generality of conclusions}
\begin{reviewer}
The authors make some claims about the conclusions from this work being extensible to molecular property prediction in general (e.g.\ in the title, pg.\ 1 line 35 in abstract, pg.\ 39 line 34 in conclusion). While this work seems to give evidence to the authors' hypothesis regarding the locality of target properties, I think these statements go too far given that the work does not validate this hypothesis in any domains other than optical properties. The work frames UV absorption as ``an ideal benchmarking target'' (pg.\ 3 line 25), and while the stated reasons do indeed make UV absorption a particularly interesting benchmarking task, I'm not sure that it's appropriate to say that it's ``ideal'' in the sense that conclusions that we draw from benchmarking on UV absorption can necessarily be applied more generally in molecular property prediction.
\end{reviewer}

\response{We agree with the reviewer that our generality claims were not supported by the experiments reported in this manuscript, which evaluate only UV absorption $\lambda_{\max}$. We have removed all references to ``general molecular property prediction'' from the title, abstract, introduction, and conclusion, softened the ``ideal benchmarking target'' framing, and recast the locality argument as a testable hypothesis whose cross-property validation is left as future work.}

\begin{changes}
  \item \textbf{Title.} Removed the trailing clause ``and Insights for General Molecular Property Prediction.'' The title now reads: ``When Do Simple Models Win? Machine Learning Architectures for UV Absorption Prediction.''
  \item \textbf{Abstract (closing paragraph).} Replaced the paragraph beginning ``These findings provide general guidance for molecular property prediction\ldots'' with a UV-scoped restatement and an explicit hypothesis sentence: ``Within UV absorption prediction, optimal model choice depends on the relationship between target compounds and available training data: simple fingerprint-based models are an efficient and accurate alternative to deep learning for interpolation within known chemical space, while learned representations from graphs, sequences, or pretraining generalize better to novel scaffolds. We further hypothesize that this pattern\ldots\ may extend to other locally determined molecular properties; validating this across property types is left as future work.''
  \item \textbf{Introduction (``ideal benchmarking target'').} Softened the framing from ``The UV absorption prediction task presents several distinctive challenges that make it an ideal benchmarking target'' to ``The UV absorption prediction task is well-suited for systematic architecture comparison for several reasons.''
  \item \textbf{Conclusion (``More broadly'' paragraph).} Removed the three sentences claiming generality to molecular property prediction beyond UV absorption and replaced them with: ``Our results raise a testable hypothesis about model selection: that architectures whose inductive bias matches the locality of the target property will outperform global-attention architectures in-distribution, while pretrained representations can confer out-of-distribution advantages even when the architecture's inductive bias is mismatched, as ChemBERTa illustrates in this work. The present work establishes this pattern for UV absorption $\lambda_{\max}$; whether it extends to fluorescence emission, lipophilicity, aqueous solubility, or other molecular endpoints whose locality differs from UV absorption requires dedicated cross-property benchmarking, which we identify as a priority for future work.''
  \item \textbf{Supporting Information title.} Updated the SI title to match the revised main-manuscript title (``Supporting Information: When Do Simple Models Win? Machine Learning Architectures for UV Absorption Prediction''), dropping the trailing ``and Insights for General Molecular Property Prediction'' clause.
  \item The existing Future Work section already frames cross-property extension as a hypothesis to validate; it is retained without further change.
\end{changes}

\subsection*{Comment R1-2: ``RF is optimal'' is hurt by the absence of a statistically significant difference between RF and Chemprop}
\begin{reviewer}
The claim that RF is ``optimal'' (pg 1 line 51 in abstract) is hurt by the fact that the difference between RF and Chemprop is not statistically significant.
\end{reviewer}

\response{We agree. We have removed every instance of ``RF is optimal'' or equivalent language and replaced it with statements that explicitly acknowledge the statistical equivalence between RF and the D-MPNN. After adopting the Greenman/Song duplicate-handling protocol (in response to reviewer concern R1-8) and retraining all five model families on the resulting v3 cleaned dataset, the RF--Chemprop paired $t$-test $p$-value moved from $0.77$ on the original v2 first-occurrence-dedup data to $0.16$ on v3; in both cases the null hypothesis of equal mean RMSE cannot be rejected at $\alpha = 0.05$, so the statistical-tie language is preserved. We are explicit in the manuscript about the v2-to-v3 shift and note that the entire RF nominal advantage on v3 is driven by a single Chemprop fold (fold~4, RMSE = 39.21~nm) whose early-stopping behaviour produced an outlier; Chemprop's fold-to-fold standard deviation on v3 (3.27) is comparable to its v2 std (3.18). \textbf{[Amended in light of R1-12.]} In response to reviewer R1-12 we subsequently ran a budget-matched 25-trial Optuna probe of Chemprop's hyperparameters and retrained the best configuration on all five v3 folds; the tuned Chemprop achieves $28.83 \pm 1.32$~nm RMSE (a $4.32$~nm mean improvement over default, paired-$t$ $p = 0.074$, Wilcoxon $p = 0.125$), with the fold-4 outlier eliminated (39.21 $\to$ 28.46~nm) and the standard deviation reduced from $3.27$ to $1.32$. This new evidence shows that the high default variance is \emph{not} intrinsic to Chemprop on this dataset but reflects undertraining of the published defaults, a finding we have added to the Limitations section. The tuned-vs-RF paired comparison gives $\Delta = -2.67$~nm with a parametric $p = 0.044$ but a non-parametric Wilcoxon $p = 0.125$ (four of the five paired differences favour tuned Chemprop, the fifth favours RF by $0.37$~nm; the minimum achievable Wilcoxon $p$ for $n = 5$ is $0.0625$). We therefore retain the statistical-tie language in the main text, add tuned Chemprop as a supplementary row in Table~\ref{tab:model_comparison} for transparency, keep the default Chemprop as the bolded primary for convention-matching with published baselines, and report the full HPO probe in new SI Section~S13. The revised text frames RF's advantage as one of compute cost and operational simplicity rather than predictive accuracy. \textbf{An additional finding from the R1-9 chromophore-overlap analysis reinforces this point}: when the test set is restricted to chromophores absent from the training set, the RF$\approx$Chemprop tie also holds (RF novel-subset RMSE = $49.5 \pm 2.9$~nm vs.\ Chemprop novel-subset RMSE = $51.9 \pm 4.7$~nm; paired $t$-test $t = -2.51$, $p = 0.066$, $n = 5$ folds), so the statistical tie between them is not an artefact of chromophore overlap in the standard cross-validation protocol. The substantial overall novel-subset RMSE (50--57~nm for all four local-feature models) is a separate matter discussed in the Limitations section and in the response to R1-9.}

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{results/r1_2_summary_figure.png}
\caption*{\textbf{Visual summary of the R1-2 response.} \emph{Panel A:} per-fold test RMSE on the v3 cleaned Joung+Beard primary dataset for RF (tuned), Chemprop with published defaults, and Chemprop after the R1-12 budget-matched 25-trial Optuna probe. The default-Chemprop fold-4 RMSE of 39.21~nm is the early-stopping outlier the R1-12 HPO probe eliminates (drops to 28.46~nm under the tuned configuration), and the fold-to-fold standard deviation falls from 3.27 to 1.32~nm. Grey lines connect paired-fold values across models. \emph{Panel B:} paired comparisons between RF and Chemprop across three conditions (v3 full test set, default and tuned, plus the v3 chromophore-novel subset from the R1-9 leakage audit). In every condition the conservative non-parametric Wilcoxon signed-rank test fails to reject the null at $\alpha = 0.05$; the parametric paired-$t$ test is borderline ($p = 0.044$) only on the tuned vs.\ RF comparison and remains non-significant on the other two. The statistical tie between RF and Chemprop is therefore robust to (i) hyperparameter tuning of Chemprop, (ii) the chromophore-overlap structure of the standard CV protocol, and (iii) the choice of parametric vs.\ non-parametric inference at $n = 5$ folds.}
\end{figure}

\begin{changes}
  \item \textbf{Abstract (line 53).} Replaced ``RF with Morgan fingerprints is optimal: it matches the GNN\ldots'' with ``RF with Morgan fingerprints is competitive with the best deep learning model at a fraction of the compute cost: RF and the D-MPNN are statistically indistinguishable (RMSE = $31.50 \pm 1.47$ vs.\ $33.15 \pm 3.27$~nm, $p = 0.16$), and RF trains in 15 minutes on a CPU with no hyperparameter tuning while outperforming all sequence-based models.''
  \item \textbf{Introduction contributions bullet.} Replaced ``RF is optimal for in-distribution screening (fast, no GPU, no tuning needed)\ldots'' with ``on in-distribution screening, RF is statistically tied with the best deep learning model (D-MPNN, $p = 0.16$) but trains $\sim$100$\times$ faster with no hyperparameter tuning; on novel compounds, deep learning models generalize better.''
  \item \textbf{Figure caption (Training time vs.\ performance).} Replaced ``RF achieves the best RMSE in 15~minutes\ldots'' with ``RF attains performance statistically tied with the best deep learning model in 15~minutes\ldots,'' eliminating the implicit optimality claim while preserving the compute-cost comparison the figure is designed to convey.
  \item \textbf{[R1-8 follow-up.]} All RF/Chemprop RMSE/MAE/R$^2$ numbers in the abstract, Table 1 (Model Comparison), Section~5.1 narrative, the conceptual locality figure caption, Section~9 Conclusion bullet 1, and the Limitations BiGRU sentence were updated to the v3 values: RF $= 31.50 \pm 1.47$, Chemprop $= 33.15 \pm 3.27$, paired $p = 0.16$. Per-fold tables S1, S3, S4, S5, S7 in the Supporting Information were correspondingly replaced with v3 values, and the Statistical Significance Table S8 now reports v3 paired-$t$ values throughout.
\end{changes}

\subsection*{Comment R1-3: ``3D methods offering advantages'' typo}
\begin{reviewer}
Pg.\ 3 line 39: ``3D methods offering advantages primarily for structurally diverse datasets (see Cross-Dataset Benchmarking)'' --- Is this a typo referring to 3D methods? This work doesn't use 3D methods, as discussed in the limitations and future work sections.
\end{reviewer}

\response{The reviewer is correct. The phrase was a leftover from an earlier draft and is inaccurate. We have removed the ``3D methods offering advantages\ldots (see Cross-Dataset Benchmarking)'' clause, corrected ``1D representations'' to ``1D and 2D representations'' to accurately describe the architectures benchmarked (which include the D-MPNN operating on molecular graphs), and explicitly state that 3D methods are not evaluated in this work, with a pointer to the Limitations and Future Work sections.}

\begin{changes}
  \item \textbf{Introduction bullet (Representation diversity).} The bullet now reads: ``Although absorption fundamentally depends on electronic structure influenced by molecular geometry, our results show that 1D and 2D representations (Morgan fingerprints, SMILES strings, molecular graphs) capture the dominant structure--property relationships for UV absorption without requiring explicit 3D coordinates. Methods that incorporate 3D conformational information are not evaluated in this work and are discussed in the Limitations and Future Work sections.''
\end{changes}

\subsection*{Comment R1-4: ``Architecture-independent solvent encoding'' mischaracterization}
\begin{reviewer}
Pg.\ 4 line 33: I'm not sure if it's appropriate to say ``Architecture-independent solvent encoding'' when some architectures use Morgan fingerprints to represent solvents and others learn the representation. If I understand correctly, the only thing that's architecture independent is that the solvent representation is simply concatenated with the solute representation rather than using some more complicated process to combine them.
\end{reviewer}

\response{The reviewer is correct. Each architecture uses its native representation for both solute and solvent (Morgan fingerprints for the tree ensembles, character-level SMILES for the sequence models, and separate D-MPNN graph encoders for Chemprop). What is consistent across architectures is the concatenation \emph{strategy}, not the encoding itself. We have rephrased the contribution accordingly. The detailed Solvent Information Encoding section already describes each architecture's native encoding scheme, so no change is needed there.}

\begin{changes}
 \item \textbf{Introduction contributions bullet.} Replaced ``Architecture-independent solvent encoding: a simple concatenation strategy yields 6--8\% RMSE reductions\ldots'' with ``Concatenation-based solvent encoding generalizes across architectures: the same strategy of concatenating native solute and solvent representations (Morgan fingerprints for the tree ensembles, SMILES strings for the sequence models, and molecular graphs for the D-MPNN) yields 6--8\% RMSE reductions across all three architecture families ($p < 0.02$), without domain-specific descriptor engineering.''
\end{changes}

\subsection*{Comment R1-5: Non-local compounds (D-A, D-A-D, long wavelength) subset analysis}
\begin{reviewer}
The idea that absorption is a locally determined property is right to a first approximation, but in some cases (e.g.\ longer-wavelength dyes, donor-acceptor or donor-acceptor-donor dyes, etc.) global effects, charge transfer, and 3D geometry become more important. It would be insightful if the authors could identify any compounds in their dataset where non-local effects would be expected to be important, and see if these results lend more support to their claims about locality and architecture choice.
\end{reviewer}

\response{We thank the reviewer for this suggestion, it is exactly the kind of test that can either strengthen or honestly weaken the locality argument. We have added a new Results subsection (``Performance on Non-Local Subsets,'' immediately preceding the Error Analysis subsection) that partitions the Joung+Beard test folds into subsets where non-local effects are expected to dominate and reports per-model error metrics on each subset. The subsets are: long-wavelength compounds ($\lambda_{\max} > 500$~nm and $> 600$~nm), donor--acceptor (D-A) molecules, donor--acceptor--donor (D-A-D) molecules, azo dyes (Ar--N$=$N--Ar), and BODIPYs (BF$_2$ bridging two ring nitrogens). Donors and acceptors are identified via 7 and 12 SMARTS rules respectively (full patterns in Supporting Information Tables~S10--S12, with the acceptor rule generalised to match aromatic-or-alkene $sp^2$ anchors so that push--pull cinnamic acids such as ferulic acid are captured). The union of all non-local categories covers 49\% of the v3 cleaned primary dataset (9{,}057 / 18{,}415 molecules).

The results give mixed support for the locality argument. On the full dataset, RF and Chemprop are statistically indistinguishable (RMSE = 31.50 vs.\ 33.15~nm, $p = 0.16$ on v3, as previously reported in our response to R1-2 and R1-8). On the long-wavelength subsets, where non-local effects are most strongly expected, Chemprop pulls clearly ahead of RF: by 4.8~nm on $\lambda > 500$~nm (47.0 vs.\ 51.8) and 6.4~nm on $\lambda > 600$~nm (63.2 vs.\ 69.5). On the broader D-A and D-A-D subsets Chemprop retains a smaller advantage (0.5~nm on D-A, 1.2~nm on D-A-D); these margins narrowed relative to the v2 first-occurrence-dedup data (1.2 and 2.4~nm respectively) because Chemprop's fold-to-fold variance widened slightly on v3 (std~$=$~4.77 on D-A, 5.83 on D-A-D), but the qualitative direction (Chemprop $\geq$ RF on D-A-style compounds) is preserved. Two subsets flip from the v2 picture: RF beats Chemprop on the small azo set (n~$=$~53; 33.8 vs.\ 37.4~nm, within one fold std of either model so not a robust reversal) and RF maintains an edge on BODIPYs (21.6 vs.\ 22.8~nm), a class whose rigid, conserved scaffold means that absorption is largely explained by short-range substitution patterns that fingerprints already capture. We are explicit about this nuance in the revised manuscript.

In the MAE column (Supporting Information Table~S14), the picture is more nuanced: Chemprop only wins on MAE for the long-wavelength subsets, while RF has lower MAE on D-A, D-A-D, BODIPYs, stilbenes, cyanines, and now also azo on v3. This reflects RF's tendency to produce residual distributions concentrated near zero with a longer tail of larger errors, which is penalised more under squared-error than under absolute-error. The reviewer's intuition is therefore borne out specifically for the squared-error metric on long-wavelength chromophores.

The first trend (all five architectures degrading sharply on $\lambda > 500$~nm) is consistent with prior observations by Beard et al.\ (2019) and Joung et al.\ (2020--2021) on the same datasets, and stems from data scarcity at long wavelengths combined with the greater structural complexity of low-gap chromophores (extended polyenes, push--pull cyanines, BODIPYs, phthalocyanines). ChemBERTa exhibits an extreme version of this failure mode: its predictions collapse to an essentially constant value around 600~nm, giving negative $R^2$ on both long-wavelength subsets (R$^2$ = $-0.68$ on $\lambda > 500$, $-4.97$ on $\lambda > 600$). This is the same prediction ceiling visible in Figure~5 (parity plot) and addressed separately in our response to R1-16.

All numbers in the new subsection are reproducible from the per-fold v3 predictions distributed with the code via\\
\hspace*{1em}\texttt{python3 analyze\_nonlocal\_subsets.py --v3 --regen\_xgboost}\\
which writes the underlying CSVs to \texttt{results/nonlocal\_subsets\_v3/}.}

\begin{changes}
  \item \textbf{Results, new subsection.} Added ``Performance on Non-Local Subsets'' as a new subsection of Results and Discussion, immediately preceding ``Error Analysis.'' The subsection defines five non-local subsets via SMARTS, reports an in-text RMSE table (Table~5 in the revised manuscript), and presents the two-trend discussion summarised above.
  \item \textbf{Supporting Information, new section.} Added ``SMARTS Rules and Metrics for Non-Local Subset Analysis'' (Section~S7) containing five tables: the donor SMARTS list (Table~S10), the acceptor SMARTS list (Table~S11), the dye-motif SMARTS list (Table~S12), the full-dataset subset counts (Table~S13), and a complete RMSE/MAE/$R^2$ breakdown across all subsets and all five models (Table~S14).
  \item \textbf{Code release.} Added \texttt{analyze\_nonlocal\_subsets.py} to the repository: it loads the saved per-fold predictions for the four DL/ensemble models, re-scores the XGBoost models from their joblib files, applies the SMARTS classification, and writes the per-fold and aggregated CSVs that the manuscript tables are built from.
\end{changes}

\subsection*{Comment R1-6: Cite prior solvent-encoding benchmark (Greenman et al., 2022)}
\begin{reviewer}
Page 8 line 41: ``but systematic evaluation of solvent encoding strategies for UV absorption prediction has been limited.'' --- Prior work benchmarked 4 different solvent encoding strategies for UVVis absorption (\url{https://doi.org/10.1039/d1sc05677h})
\end{reviewer}

\response{We thank the reviewer for pointing out this oversight; the cited paper is Greenman, Green, and G\'omez-Bombarelli, ``Multi-fidelity prediction of molecular optical peaks with deep learning,'' \textit{Chem.\ Sci.} \textbf{2022}, \textit{13}, 1152--1162 (DOI 10.1039/D1SC05677H). It is the most directly relevant prior benchmark of solvent-encoding strategies for UV/Vis absorption and our previous text understated its contribution. We have added a Related Work paragraph that names the four strategies Greenman et al.\ compared (no solvent; one-hot solvent labels; Morgan-fingerprint concatenation; a second D-MPNN encoder for the solvent), and rephrased the contribution claim so that we no longer assert that systematic evaluation has been ``limited.'' Instead, we now state that Greenman et al.\ established that explicit solvent representation matters \emph{within a D-MPNN architecture} for UV/Vis peak prediction, and that the open question we address here is the comparative behaviour of fingerprint- and sequence-based architectures under the same concatenation-based scheme on a shared dataset. The Greenman et al.\ reference is also cited later in our response to R1-8 (duplicate handling) and R1-9 (chromophore-solvent leakage), where their methodology is directly relevant.}

\begin{changes}
  \item \textbf{Related Work (Solvent effects, pg.\ 8 line 41).} Replaced ``but systematic evaluation of solvent encoding strategies for UV absorption prediction has been limited'' with a new two-sentence anchor that introduces Greenman et al.~\cite{greenman2022multifidelity}, names the four solvent-encoding strategies they benchmarked, and reframes our contribution as a cross-architecture rather than novel-strategy comparison.
  \item \textbf{Bibliography.} Added \texttt{greenman2022multifidelity} (Greenman, Green, G\'omez-Bombarelli, \textit{Chem.\ Sci.} \textbf{2022}, \textit{13}, 1152) to \texttt{Proposal.bib}. Also added \texttt{song2025fluortools} (Song et al., \textit{Digital Discovery} \textbf{2025}, \textit{4}, 3728) in anticipation of our responses to R1-7 and R1-8, where their Table 3 of UV absorption datasets and their duplicate-handling protocol are directly relevant.
\end{changes}

\subsection*{Comment R1-7: Deep4Chem overlap with Joung; choice of Joung+Beard as primary dataset}
\begin{reviewer}
The Deep4Chem dataset isn't a great ``external dataset'' given that it substantially overlaps with the Joung dataset that makes up part of the primary dataset. Why were the Joung and Beard datasets chosen to make the primary dataset as opposed to the many other UV absorption datasets that exist (see Table 3 here: \url{https://doi.org/10.1039/D5DD00402K})?
\end{reviewer}

\response{The reviewer makes two related points, both of which we accept and address with new analysis and text.

\textbf{(a) On the Deep4Chem overlap.} The reviewer is correct: Deep4Chem was the source dataset for the Joung half of our primary collection, so by construction it shares a very high fraction of records with Joung+Beard. To replace hand-waving with a number, we canonicalised the SMILES of both datasets through RDKit and computed set-intersections at three levels of granularity. The results, now reported in Supporting Information Section~S8 (Table~S15 and Figure~S3), are:
\begin{itemize}[leftmargin=2em,itemsep=2pt]
  \item \emph{Unique canonical solute SMILES:} 5{,}018 of Deep4Chem's 6{,}480 unique solutes (77.4\%) also appear in Joung+Beard. Conversely, 63.8\% of Joung+Beard solutes appear in Deep4Chem.
  \item \emph{Unique (solute, solvent) pairs (both canonicalised):} 12{,}654 of Deep4Chem's 17{,}427 pairs (72.6\%) match a pair in Joung+Beard exactly.
  \item \emph{Generic Bemis--Murcko scaffolds:} 2{,}467 of Deep4Chem's 2{,}498 scaffolds (98.8\%) are also present in Joung+Beard; the two datasets explore essentially the same scaffold space.
\end{itemize}
These numbers vindicate the reviewer's intuition that Deep4Chem is poorly characterised as an OOD external benchmark. We have updated the Methods (Dataset Description and Preparation) to state explicitly that ``Deep4Chem is therefore best characterised as an \emph{in-distribution external} benchmark, useful for reproducing published per-paper baselines under their own splits, but \emph{not} a test of out-of-distribution generalisation.'' We have also added a sentence to the Cross-Dataset Benchmarking subsection noting that the Deep4Chem results should be read as in-distribution validation, and that the Jung 2024 results (which we also report) constitute the more rigorous external test because Jung 2024 has only 48.8\% solute overlap with Joung+Beard, a separate experimental provenance, and distinct chromophore families (notably long-wavelength dyes from the solar-cell and OLED literature).

\textbf{(b) On the choice of Joung+Beard as the primary dataset.} The reviewer rightly points out that Song et al.~\cite{song2025fluortools} survey nine UV/Vis dye datasets in their Table~3 (ChemFluor, Deep4Chem, SMFluo, ChemDataExtractor, Dye aggregation, DSSCDB, Fluor-predictor, Ksenofontov's data, Literature retrieval). We have added a four-part rationale to the Methods section explaining why Joung+Beard was selected as the primary dataset:
\begin{itemize}[leftmargin=2em,itemsep=2pt]
  \item It is the largest unified collection of \emph{purely experimental} solute--solvent $\lambda_{\max}$ measurements available without paywall or license restrictions at the time of submission, with each record carrying an experimental wavelength and an explicit solvent label.
  \item It has \emph{published baselines from independent groups} (Joung et al.\ GCNN on Deep4Chem, Beard et al.\ DR-CNN, Jung et al.\ multifidelity and GBFS) that enable apples-to-apples comparison without training every baseline ourselves.
  \item Combining the two source collections yields broader chromophore coverage than either alone (Deep4Chem 6{,}480 and ChemFluor 4{,}386 unique solutes vs.\ approximately 7{,}000 in the curated Joung+Beard primary set after our preprocessing).
  \item The dataset is dominated by drug-like organic chromophores rather than the dye-aggregation or solar-cell-specific compositions of DSSCDB and Dye-aggregation, matching our intended downstream applications (photosafety screening and wetlab UV-absorber discovery).
\end{itemize}
We considered but did not include Mamede's MEC dataset in the primary collection because of the Reaxys licence restrictions that prevent direct redistribution (see our response to R1-15); we use it instead as a downstream photosafety-classification benchmark where the labels and not the raw $\lambda_{\max}$ values are the supervision target. Computational datasets (e.g.\ Fluor-predictor's TD-DFT calculations) were excluded so that the primary regression target is purely experimental.

The script that produces the overlap numbers (\texttt{analyze\_deep4chem\_overlap.py}) is distributed with the code, so anyone can re-run the analysis or extend it to other dataset pairings.}

\begin{changes}
  \item \textbf{Methods (Dataset Description and Preparation, pg.\ 8).} Added a new four-part rationale paragraph immediately after the opening sentence, explaining why Joung+Beard was chosen as the primary dataset and citing Song et al.\ for the dataset-landscape survey.
  \item \textbf{Methods (Dataset Description and Preparation, pg.\ 8).} Extended the Deep4Chem description to report the measured overlap (77.4\% of solutes, 72.6\% of pairs) and to reframe Deep4Chem explicitly as an in-distribution rather than OOD external benchmark, with a pointer to the new SI overlap analysis (Section~S8, Figure~S3).
  \item \textbf{Methods (Dataset Description and Preparation, pg.\ 8).} Extended the Jung 2024 description to note the 48.8\% solute overlap and the qualitatively different chromophore coverage that makes it the more rigorous of the two external benchmarks.
  \item \textbf{Results (Cross-Dataset Benchmarking, pg.\ 30).} Added a caveat sentence at the end of the section noting that the Deep4Chem results reflect in-distribution interpolation rather than OOD generalisation, and that Jung 2024 carries the OOD claim.
  \item \textbf{Supporting Information, new section.} Added ``Overlap Between Primary and Deep4Chem Datasets'' (Section~S8) containing the per-granularity overlap table (Table~S15) and a side-by-side Venn diagram of solutes and (solute, solvent) pairs (Figure~S3); reproduced exactly by the supplied \texttt{analyze\_deep4chem\_overlap.py} script.
  \item \textbf{Figure renumbering housekeeping.} Insertion of the new Figure~S3 (Venn diagram) shifts the existing BiGRU character-type saliency figure from S3 to S4; the three main-text references to ``Figure~S3'' that pointed to the saliency analysis have been updated to ``Figure~S4'' accordingly.
  \item \textbf{Methods (Dataset Description and Preparation, pg.\ 8), clarification of which Song-Table-3 datasets were considered.} Appended two sentences to the rationale paragraph distinguishing the absorption-only alternatives from the emission datasets in Song's Table~3: ``Among Song's Table 3 entries, several (ChemFluor, SMFluo, Fluor-predictor) measure fluorescence emission rather than UV absorption and are therefore not direct alternatives for our $\lambda_{\max}$ regression task; ChemDataExtractor and Ksenofontov's data provide smaller NLP-extracted or curated subsets that we did not adopt as primary. The Mamede et al.\ Reaxys-derived dataset provides binary photosafety labels rather than continuous $\lambda_{\max}$ values, and is used in this work as the basis for the photosafety classification benchmark rather than as a primary regression source.''
\end{changes}

\subsection*{Comment R1-8: Duplicate-handling protocol (Greenman et al.\ / Song et al.)}
\begin{reviewer}
Why was the first occurrence retained when duplicates were identified? Prior work (\url{https://doi.org/10.1039/d1sc05677h} and \url{https://doi.org/10.1039/D5DD00402K}) has taken a more robust approach by dropping all entries with large discrepancies ($>$ 5 nm) while taking the average for small discrepancies ($<$ 5 nm).
\end{reviewer}

\response{We accept this concern and have adopted the Greenman/Song duplicate-handling protocol for the revised manuscript. The protocol groups records by (canonical solute SMILES, canonical solvent SMILES) and, for each multi-record group, averages the $\lambda_{\max}$ values when the within-group spread $\max - \min \leq 5$~nm or drops the entire group when the spread exceeds 5~nm; singletons are unchanged.

\textbf{Analysis-first.} Before committing to retraining we quantified what the protocol would do to our data (Supporting Information Section~S9 and the script \texttt{analyze\_duplicate\_handling.py}). Of the 18{,}755 records that pass dropna and SMILES validation, 18{,}162 are singletons, 253 duplicate groups (covering 508 records) have spread $\leq 5$~nm and were averaged, and 41 duplicate groups (84 records) have spread $> 5$~nm and were discarded. The resulting cleaned dataset (UV\_canonical\_v3\_dedup.csv) contains 18{,}415 records, a 1.81\% reduction from the original 18{,}755. The discarded groups are concentrated in the 300--500~nm range where the original dataset is most densely populated; the long-wavelength tail ($\lambda > 500$~nm) loses essentially no records to the 5~nm rule, so the cleaning protocol does not erode the already-scarce deep-coloured chromophores that drive the non-local subset analysis (R1-5). Within the duplicate groups, the within-group spread distribution is starkly bimodal: 86\% of groups have spread $= 0$~nm (cross-listed records reporting the same value) and the remaining 14\% are spread across a fat tail out to 100~nm. The high-disagreement groups are concentrated in volatile / water-miscible solvents (DCM 28\% drop rate, MeOH 15\%, EtOH 18\%), consistent with experimental noise rather than dataset mislabelling.

\textbf{Full retrain on v3.} We re-trained all five model families on the v3 cleaned dataset using stratified 5-fold cross-validation with the same seed (7) and the same hyperparameters as our original v2 submission. Per-fold predictions, models, and configs are saved with a \texttt{\_v3\_} prefix in \texttt{results/}, and an aggregated v2-vs-v3 comparison is presented in Supporting Information Table~S16 and reproduced here:

\begin{center}\small
\begin{tabular}{lcccc}
\toprule
\textbf{Model} & \textbf{v2 RMSE (nm)} & \textbf{v3 RMSE (nm)} & $\boldsymbol{\Delta}$ & \textbf{Note} \\
\midrule
RF (tuned)           & $31.34 \pm 1.82$ & $31.50 \pm 1.47$ & $+0.16$ & stable; std tightens \\
XGBoost (MSE)        & $33.70 \pm 1.61$ & $33.92 \pm 1.26$ & $+0.22$ & stable; std tightens \\
Chemprop (D-MPNN)    & $31.69 \pm 3.18$ & $33.15 \pm 3.27$ & $+1.46$ & fold-4 outlier (39.21) \\
BiGRU (tuned)        & $34.71 \pm 1.40$ & $36.20 \pm 1.20$ & $+1.49$ & higher stochasticity per fold \\
ChemBERTa            & $50.39 \pm 2.30$ & $54.09 \pm 3.57$ & $+3.70$ & still worst; pathology persists \\
\bottomrule
\end{tabular}
\end{center}

\textbf{Headline conclusion preserved.} The RF~$\approx$~Chemprop paired $t$-test $p$-value moves from $0.77$ on v2 to $0.16$ on v3, but in both cases the null hypothesis of equal mean RMSE is not rejected at $\alpha = 0.05$, so the statistical-tie claim remains valid. The qualitative model ranking (RF $\leq$ Chemprop $<$ XGBoost $<$ BiGRU $\ll$ ChemBERTa) is unchanged. The 1.65~nm nominal RF advantage on v3 is driven by a single Chemprop fold (fold~4, RMSE = 39.21~nm) that appears to be an early-stopping outlier; Chemprop's overall v3 std (3.27) is comparable to v2 (3.18), so the variance is intrinsic to the architecture rather than protocol-driven. We disclose this in the Results discussion.

\textbf{Total wall-clock cost.} The full retrain (XGBoost + RF + BiGRU tuned + Chemprop D-MPNN + ChemBERTa, 5 folds each) used $\sim$15.4 hours of A40 GPU/CPU time over $\sim$18 hours of orchestrator wall-clock; resume-safe per-fold artefacts (\texttt{<model>\_v3\_fold<i>\_progress.json}, \texttt{predictions.npy}, \texttt{y\_test.npy}, \texttt{test\_indices.npy}, \texttt{metrics.json}) are saved alongside the v2 files.

The cleaned dataset (\texttt{previous\_code/UV\_canonical\_v3\_dedup.csv}) and the builder script (\texttt{build\_v3\_dataset.py}, with a companion README) are distributed with the code so anyone can reproduce or extend the protocol.}

\begin{changes}
  \item \textbf{Methods (Dataset Description and Preparation, pg.\ 8).} Replaced the ``retain first occurrence'' description in step (3) of the data-curation list with an explicit description of the Greenman--Song protocol, citing \texttt{greenman2022multifidelity} and \texttt{song2025fluortools}, and reporting the audit numbers (18{,}162 singletons / 253 averaged groups / 41 dropped groups $\to$ 18{,}415 records). Updated the dataset-summary sentence to ``18{,}415 solute--solvent pairs spanning 7{,}846 unique canonical chromophores and 726 unique canonical solvent SMILES,'' the $\lambda_{\max}$ range to ``162--1100~nm'' with median $396$~nm, and the corresponding dataset-summary table (Table~\ref{tab:dataset_summary}). Also corrected the previous incorrect counts (``7{,}016 chromophores'' and ``365 solvents'' from the original preprint, which understated the canonical-SMILES diversity).
  \item \textbf{Results (Table~1 Model Comparison and discussion paragraphs, pg.\ 25--26).} Replaced all five RF / XGBoost / Chemprop / BiGRU / ChemBERTa rows with v3 values; updated the dataset annotation in the caption to ``18,415 samples after Greenman--Song duplicate handling'' and added a caption note that the qualitative ranking and the RF$\approx$Chemprop tie are preserved (with a pointer to SI Section~S9 for the full v2-vs-v3 comparison). Updated the narrative numbers: ``RMSE = 31.50 and 33.15~nm, $p = 0.16$''; ``RF--Chemprop difference (5.2\%) is not statistically significant''; ``RF vs.\ tuned BiGRU significant ($t = -22.5$, $p < 0.001$)''. Footnoted the two ablation rows (BiGRU + Solvent default, XGBoost MAE) that remain at v2 because their v3 retraining was outside the scope of this revision.
  \item \textbf{Results (Solvent ablation table caption, pg.\ 27).} Added a caption note that the ablation is reported on v2 because both arms need to be on the same dataset; pointed to SI Section~S9 for the cross-protocol spot-check.
  \item \textbf{Results (Conceptual locality figure caption boxes, pg.\ 26).} Updated the per-model RMSE annotations on the figure (RF 31.50, Chemprop 33.15, BiGRU 36.20, ChemBERTa 54.09).
 \item \textbf{Results (Non-Local Subsets subsection, pg.\ 30; R1-5 follow-up).} Replaced the table with v3 numbers and rewrote the two-paragraph discussion. Headline finding (Chemprop pulls ahead of RF on long-wavelength subsets, with a small or reversed advantage on local-context subsets) is preserved but quantitatively softened (azo flips to RF; BODIPY edge widens slightly to RF; D-A and D-A-D margins narrow). The v3 subset analysis is reproducible via \texttt{python3 analyze\_nonlocal\_subsets.py --v3 --regen\_xgboost}, which writes to \texttt{results/nonlocal\_subsets\_v3/}.
  \item \textbf{Results (Cross-Dataset Benchmarking, pg.\ 30).} Unchanged because external benchmarks train on the external datasets' own splits, not on the v2/v3 primary dataset.
  \item \textbf{Results (Wetlab Validation closing sentence, pg.\ 36).} Updated the cross-validated MAE comparisons in the closing sentence: ``ChemBERTa: 26.3 vs.\ 26.8~nm MAE on v3; \ldots RF: 38.5 vs.\ 15.4~nm on v3''.
  \item \textbf{Conclusion bullet~1, pg.\ 39.} Updated to ``RMSE = 31.50 and 33.15~nm, $p = 0.16$'', ``tuned BiGRU (36.20~nm) and ChemBERTa (54.09~nm)''.
  \item \textbf{Limitations BiGRU sentence, pg.\ 39.} Updated to acknowledge that v3 yields tuned BiGRU $= 36.20 \pm 1.20$~nm vs.\ RF $= 31.50$~nm.
  \item \textbf{Abstract (pg.\ 1) and Introduction contributions bullet (pg.\ 3).} See R1-2 amendment for the v3 numerical updates.
  \item \textbf{Supporting Information per-fold tables (S1, S3, S4, S5, S7).} Replaced with v3 values. Tables S2 and S6 (default BiGRU + Solvent and Chemprop nosolvent) remain at v2 as noted in their captions because v3 retraining was outside scope.
  \item \textbf{Supporting Information Table~S8 (statistical significance).} Replaced with v3 paired-$t$ values: RF vs.\ Chemprop $p = 0.164$, RF vs.\ BiGRU $p < 0.001$, etc. Solvent vs.\ no-solvent rows remain at v2 with caption flag.
  \item \textbf{Supporting Information Tables S13 and S14 (non-local subset analysis).} Replaced with v3 subset counts and v3 RMSE/MAE/$R^2$ breakdown.
  \item \textbf{Supporting Information, new Section~S9.} Added ``v2-vs-v3 Cross-Protocol Comparison (R1-8)'' with Table~S16 reporting side-by-side v2/v3 metrics for all five model families plus a four-point commentary on what the comparison shows.
  \item \textbf{Code release.} Added \texttt{analyze\_duplicate\_handling.py} (Step 0 audit), \texttt{build\_v3\_dataset.py} (creates the cleaned CSV with companion README), \texttt{retrain\_v3.py} (orchestrator with per-fold resume), and patched \texttt{run\_chemprop.py} / \texttt{run\_chemberta.py} with the \texttt{CHEMPROP\_DATA\_PATH} / \texttt{CHEMBERTA\_DATA\_PATH} / \texttt{...\_FOLD\_INDICES\_PATH} / \texttt{...\_PREFIX} environment-variable overrides used by the orchestrator. Per-fold v3 artefacts (\texttt{<model>\_v3\_fold<i>\_predictions.npy}, \texttt{\_y\_test.npy}, \texttt{\_test\_indices.npy}, \texttt{\_metrics.json}, plus joblib/keras/ckpt/bin checkpoints) are saved alongside the v2 files.
\end{changes}

\subsection*{Comment R1-9: 64/16/20 split rationale and chromophore-solvent leakage}
\begin{reviewer}
Why was 64/16/20 chosen as the split fractions? These numbers are a bit unusual --- did these simply fall out of the solvent stratification process? Does this splitting strategy address the data leakage concerns raised by prior work (\url{https://doi.org/10.1039/d1sc05677h}) for the same chromophore appearing in both train and test sets in different solvents?
\end{reviewer}

\response{The reviewer's question has two parts and we address both.

\textbf{(a) 64/16/20 origin.} The reviewer is correct that these fractions are not chosen arbitrarily, they fall out of a nested-split design. We use a stratified 5-fold outer split (80\% train+val / 20\% test per fold, stratified on solvent identity to ensure rare-solvent coverage in every test fold), composed with an inner \texttt{train\_test\_split} of the trainval portion (80\% train / 20\% val of trainval). Combining these gives 64\% train / 16\% val / 20\% test of the total. The deep learning models (BiGRU, Chemprop, ChemBERTa) use the val slice for early stopping; the fingerprint-based models (RF, XGBoost) ignore the val slice and fit on the 64\% train portion only. We have added a sentence to the Methods Evaluation Protocol making this nested-split origin explicit.

\textbf{(b) Chromophore-solvent leakage.} The reviewer is also correct that the standard stratified-CV protocol does \emph{not} address the chromophore-overlap concern Greenman et al.\ raised. We audited our v3 5-fold splits and found a substantial leakage rate: 66.8\% of test records have their canonical solute also appear in the train slice under a different solvent (69.3\% if the val slice is counted). The corresponding leak rate at the unique-solute level is 54.6\% per fold. The reviewer's concern is therefore not just theoretically valid for our protocol, it is materially present.

Table~S18 in the revised Supporting Information (new Section~S10) decomposes each model's test-fold RMSE into chromophore-seen and chromophore-novel subsets. The decomposition is striking: the chromophore-seen subset gives RMSEs of 16--45~nm, the chromophore-novel subset gives 50--70~nm, and the $\Delta$RMSE (novel minus seen) is $+25$ to $+36$~nm across all five model families. We summarise the key reading of this analysis as follows.
\begin{itemize}[leftmargin=2em,itemsep=2pt]
 \item \textbf{All models are affected comparably.} The 25--36~nm spread of $\Delta$RMSE is narrow relative to the size of the effect itself. In particular, RF and Chemprop have $\Delta$RMSE of $+33.16$ and $+32.26$~nm respectively. A paired $t$-test on the chromophore-novel subset alone (n~=~5 folds) gives $t = -2.51$, $p = 0.066$ (still failing to reject the null at $\alpha = 0.05$) so the RF$\approx$Chemprop tie ($p = 0.16$ on the full v3 test set) is preserved when the comparison is restricted to chromophores genuinely absent from the training data.
  \item \textbf{The in-distribution numbers are upper bounds on novel-chromophore generalisation.} A model with 31.5~nm CV RMSE will not, in expectation, achieve 31.5~nm on chromophores genuinely outside its training set. The wetlab validation set (16 commercial UV-absorbers, of which 15 are absent from the training data at the SMILES level \textbf{[Qualifier added per R1-13: although 15/16 of the same molecules share a Bemis--Murcko generic scaffold with training, so the wetlab tests molecule-level rather than scaffold-class generalisation; see new SI Section~S14 and the R1-13 response]}) and the Jung 2024 external dataset ($\sim$50\% solute overlap, separate experimental provenance) are the more rigorous out-of-distribution complements; both are already in the manuscript.
  \item \textbf{The magnitude is intrinsic to the dataset shape.} With 7{,}846 unique solutes and 18{,}415 records, the dataset averages $\sim$2.35 records per chromophore, so any random or solvent-stratified split will produce high chromophore-level overlap by construction. A truly novel-chromophore evaluation requires scaffold splitting, which we address separately in R1-13.
\end{itemize}

We do not unilaterally replace Table~\ref{tab:model_comparison}'s numbers with novel-only RMSEs because the field convention (Joung, Beard, Greenman, and Jung all report aggregate stratified-CV RMSE on these data) is necessary for apples-to-apples comparison with their published baselines. Instead, we have added the chromophore-overlap caveat directly to the Methods Evaluation Protocol (pointing to SI Section~S10) and to the Limitations section (framing the in-distribution numbers as upper bounds), and we have flagged the result in the response to R1-2 because it reinforces (rather than weakens) the statistical-tie story for RF vs.\ Chemprop.

The complete analysis is reproducible from the saved v3 per-fold predictions via \texttt{python3 analyze\_chromophore\_leakage.py}, which writes \texttt{results/r1\_9\_leakage\_\{stats.json,per\_model.csv,per\_fold.csv,summary.md\}}. The leakage script is distributed with the code.}

\begin{changes}
  \item \textbf{Methods (Evaluation Protocol, pg.\ 8--9).} Added a paragraph explaining the nested-split origin of the 64/16/20 fractions (outer stratified 5-fold $\times$ inner 80/20 train/val split) and introducing the chromophore-overlap measurement: 66.8\% leak rate vs.\ train, 69.3\% vs.\ train+val, with $\Delta$RMSE of $+25$ to $+36$~nm between novel and seen subsets across all five model families. Pointer to SI Section~S10 for the full decomposition.
  \item \textbf{Limitations (pg.\ 40).} Added a bullet stating that Table~\ref{tab:model_comparison} numbers should be read as upper bounds on novel-chromophore generalisation, with wetlab and Jung 2024 as the rigorous OOD complements.
  \item \textbf{Conclusion bullet 1 (pg.\ 39).} Appended a 15-word caveat clause to the headline statistical-tie sentence: ``$p = 0.16$, see Methods and Limitations for the chromophore-overlap caveat on in-distribution CV''.
  \item \textbf{Supporting Information, new Section~S10.} Added ``Chromophore-Solvent Leakage Analysis (R1-9)'' with two tables (per-fold leakage statistics, per-model RMSE decomposition into seen vs.\ novel subsets) and a three-point discussion summarising the findings above. The existing ``BiGRU Character-Type Saliency'' section shifts from S10 to S11; the BiGRU saliency figure remains Figure~S4 because the new section adds tables only.
  \item \textbf{Bibliography.} The existing \texttt{greenman2022multifidelity} entry already added for R1-6 is now also cited in the Evaluation Protocol paragraph and SI Section~S10.
  \item \textbf{Code release.} Added \texttt{analyze\_chromophore\_leakage.py}: loads the v3 fold indices and per-fold predictions for all five model families, computes per-fold leakage rates and per-model seen-vs-novel RMSE/MAE/$R^2$, and emits the four output files cited above.
\end{changes}

\subsection*{Comment R1-10: Move architecture equations to the Supporting Information}
\begin{reviewer}
The detailed description of the architectures on pages 11--19, especially the equations, are probably not necessary to include in the main body of the paper. These may be better placed in the supporting information since the architectures are well-documented in other work, and the core messages of the paper do not depend on any of these mathematical details.
\end{reviewer}

\response{We accept this recommendation without modification. The equation blocks defining RF (ensemble prediction, MSE split criterion, Morgan fingerprint hashing), XGBoost (additive prediction, regularised objective), vanilla RNN (hidden-state update), GRU (4-equation align block of update/reset/candidate/output gates), BiGRU (forward/backward concatenation), and the D-MPNN (message-passing recurrence) have been moved verbatim into a new Supporting Information Section~S12 (``Detailed Model Architectures''). The main-text Methods Architecture subsections retain a 3--5 sentence summary per architecture that names the canonical citation governing the math, the input representation, the key hyperparameters and parameter count we use, and a pointer to the SI for the equations. The ChemBERTa and Solvent Information Encoding subsections were not affected because the former had no equation blocks at the level of detail flagged by the reviewer and the latter contains a single inline equation that defines a specific contribution of this paper (the concatenation-based solvent encoding). The BiGRU Bayesian HPO subsection and the BiGRU architecture-comparison figure (default vs.\ tuned, Figure~\ref{fig:bigru_architectures}) remain in the main text because they describe a finding rather than canonical architecture definition.

The relocation removes approximately three pages of main-text equation/explanatory prose without removing any content from the manuscript as a whole: SI Section~S12 contains the same equations and explanatory paragraphs that previously appeared in the main text, with equation labels renamed with a \texttt{si\_} prefix for clarity. None of the moved equations were cross-referenced from outside their subsections in the original manuscript, so no equation \texttt{\textbackslash eqref} calls needed updating.}

\begin{changes}
  \item \textbf{Methods (RF, XGBoost, BiGRU, D-MPNN subsections, pp.\ 11--19).} Each of the four affected architecture subsections is replaced with a 3--5 sentence summary covering: canonical citation, input representation, key hyperparameters, parameter count, and a pointer to SI Section~S12. Subsection labels (\texttt{sec:rf}, \texttt{sec:xgboost}, \texttt{sec:bigru}, \texttt{sec:chemprop}) are preserved so cross-references from elsewhere in the manuscript continue to resolve.
  \item \textbf{Methods (Pretrained Transformer / ChemBERTa subsection).} Unchanged, it had no equation blocks at the level of detail the reviewer flagged.
  \item \textbf{Methods (Solvent Information Encoding subsection).} Unchanged, the single inline equation defines the concatenation-based solvent encoding contribution and is too tightly coupled to the paper's solvent-encoding finding to move.
  \item \textbf{Methods (BiGRU HPO subsection and Figure~\ref{fig:bigru_architectures}).} Unchanged, HPO is a finding/decision rather than architectural definition, and the figure visually summarises the HPO outcome.
  \item \textbf{Supporting Information, new Section~S12.} Added ``Detailed Model Architectures'' with a one-paragraph framing introduction followed by per-architecture subsections (RF, XGBoost, BiGRU, D-MPNN) containing the moved equation blocks and the explanatory prose verbatim. Equation labels renamed with the \texttt{si\_} prefix (\texttt{eq:si\_rf\_predict}, \texttt{eq:si\_gru\_update}, etc.) to make the SI provenance obvious.
  \item \textbf{SI preamble.} Added the \texttt{\textbackslash vect} and \texttt{\textbackslash matr} shorthand commands (mirrored from the main paper) so the moved equation blocks render correctly in the SI document.
\end{changes}

\subsection*{Comment R1-11: ``We adapt'' should be ``We use'' for Chemprop multicomponent}
\begin{reviewer}
pg. 18 line 20: I think ``We adapt'' should be ``We use'' since this capability is already present in Chemprop out of the box and has been used for solute-solvent prediction for optical and other properties in many prior works.
\end{reviewer}

\response{The reviewer is correct. This change is implicitly absorbed by the R1-10 rewrite of the D-MPNN Methods subsection: the truncated paragraph now reads ``We use Chemprop's built-in multicomponent configuration for solute--solvent prediction,'' which removes the ``We adapt'' phrasing and makes it explicit that the multicomponent capability is a built-in feature of Chemprop rather than an adaptation by us. No additional edit was required.}

\begin{changes}
  \item \textbf{Methods (D-MPNN subsection).} The ``We adapt the D-MPNN for solute--solvent prediction using a multicomponent configuration'' sentence has been replaced (as part of the R1-10 truncation) with ``We use Chemprop's built-in multicomponent configuration for solute--solvent prediction.''
\end{changes}

\subsection*{Comment R1-12: D-MPNN hyperparameter sweep}
\begin{reviewer}
Did the authors do any hyperparameter optimization for the D-MPNN model? The same was done for the BiGRU, but no mention is made of any analogous procedure for the D-MPNN.
\end{reviewer}

\response{The reviewer is correct that our original submission left Chemprop at its published defaults while BiGRU received a 25-trial Optuna study. This was an asymmetry in the experimental protocol that we have now closed.

\textbf{Probe design.} We ran an Optuna study on fold 0 of the v3 cleaned Joung+Beard dataset matching the BiGRU budget exactly (25 trials, TPE sampler with $n_{\text{startup\_trials}} = 5$, MedianPruner with $n_{\text{warmup\_steps}} = 20$). The search space varied six hyperparameters: message-passing hidden dimension $d_h \in \{200, 300, 400, 600, 900\}$, depth $T \in \{3, 4, 5, 6\}$, dropout $\in \{0.0, 0.1, 0.2, 0.3\}$, FFN depth $\in \{1, 2, 3\}$, FFN hidden $\in \{300, 500, 800\}$, and batch size $\in \{32, 50, 64, 128\}$. The default configuration was enqueued as trial 0 so the study contains an in-distribution baseline. The Noam learning-rate schedule, maximum epochs (100), early-stopping patience (15 on val loss), and loss were held fixed at the values used for the default-configuration training, so the only variable was architecture/regularisation.

\textbf{Best configuration.} After 24 completed trials, the best fold-0 validation RMSE was achieved at trial 23 with $d_h = 900$, $T = 4$, dropout $= 0.1$, FFN $2 \times 800$, batch $= 32$ (5.6M parameters, $\sim$9$\times$ the 636K of the default). Three additional independent runs of the same configuration during the probe gave fold-0 val RMSE values within $\pm 1$~nm of trial 23, confirming reproducibility.

\textbf{Full 5-fold retrain.} We then retrained Chemprop at the tuned configuration on all five v3 folds using \texttt{run\_chemprop.py} with best-checkpoint restoration on val loss. The per-fold tuned-vs-default RMSEs and the resulting paired statistics are:

\begin{center}\small
\begin{tabular}{cccc}
\toprule
\textbf{Fold} & \textbf{Default RMSE} & \textbf{Tuned RMSE} & $\boldsymbol{\Delta}$ \\
\midrule
0 & 30.62 & 30.70 & $+0.08$ \\
1 & 30.53 & 28.22 & $-2.31$ \\
2 & 31.49 & 26.92 & $-4.57$ \\
3 & 33.90 & 29.84 & $-4.06$ \\
4 & 39.21 & 28.46 & $-10.75$ \\
\midrule
\textbf{Mean $\pm$ Std} & \textbf{33.15 $\pm$ 3.27} & \textbf{28.83 $\pm$ 1.32} & \textbf{$-4.32$} \\
\bottomrule
\multicolumn{4}{l}{\footnotesize Paired $t$: $t = -2.40$, $p = 0.074$; Wilcoxon: $p = 0.125$.} \\
\end{tabular}
\end{center}

The headline observation is that the improvement is concentrated almost entirely in fold 4: the default Chemprop training on fold 4 produced an early-stopping outlier (39.21~nm, $\sim$2.5 $\sigma$ above the other four folds), and the tuned configuration eliminates it (28.46~nm). The remaining four folds individually improve by 0.1 to 4.6~nm. The variance reduction (3.27 $\to$ 1.32) is therefore not a precision-bumping artefact but a substantive consequence of HPO, and it shows that the high default variance is not intrinsic to Chemprop on this dataset.

\textbf{Tuned-vs-RF comparison.} The narrative-critical question is whether the tuned Chemprop dislodges RF as the leading model. The paired comparison gives:
\begin{center}\small
\begin{tabular}{cccc}
\toprule
\textbf{Fold} & \textbf{RF RMSE} & \textbf{Tuned Chemprop} & $\boldsymbol{\Delta}$ \\
\midrule
0 & 30.33 & 30.70 & $+0.37$ \\
1 & 30.46 & 28.22 & $-2.24$ \\
2 & 30.24 & 26.92 & $-3.32$ \\
3 & 32.67 & 29.84 & $-2.83$ \\
4 & 33.81 & 28.46 & $-5.35$ \\
\midrule
\textbf{Mean $\pm$ Std} & \textbf{31.50 $\pm$ 1.47} & \textbf{28.83 $\pm$ 1.32} & \textbf{$-2.67$} \\
\bottomrule
\multicolumn{4}{l}{\footnotesize Paired $t$: $t = -2.90$, $p = 0.044$; Wilcoxon: $p = 0.125$.} \\
\end{tabular}
\end{center}
The parametric and non-parametric tests disagree: the paired $t$-test marginally rejects the null at $\alpha = 0.05$ ($p = 0.044$), but the Wilcoxon signed-rank test does not ($p = 0.125$, the minimum achievable for $n = 5$ folds is $0.0625$; four of the five paired differences favour tuned Chemprop, the fifth favours RF by $0.37$~nm). This disagreement is a known pathology of small $n$ in statistical testing: the $t$-test assumes a normal distribution of differences while the Wilcoxon makes no such assumption. We report both $p$-values and treat the result as borderline rather than as a clean rejection of the RF$\approx$Chemprop tie. Restricting to chromophores genuinely absent from the training slice (the R1-9 subset) gives tuned Chemprop $= 46.54 \pm 2.49$~nm vs.\ default Chemprop $= 51.90 \pm 4.23$~nm ($\Delta = -5.36$~nm, paired $t = -2.65$, $p = 0.057$); the HPO benefit extends to genuinely novel chromophores, but absolute novel-subset errors remain $\sim$20~nm above the full-test numbers for both configurations.

\textbf{Choice of headline row.} Three considerations led us to retain the default Chemprop as the bolded primary in Table~\ref{tab:model_comparison} and add the tuned configuration as a supplementary row rather than replacing the primary:
\begin{itemize}[leftmargin=2em,itemsep=2pt]
  \item The published Chemprop benchmarks on these data (Joung 2020--2021, Beard 2019, Greenman 2022, Jung 2024, the Chemprop v2 release paper~\cite{chempropv2_2024}) all report results at the default configuration. Apples-to-apples comparison with those baselines requires retaining the default as a headline row.
  \item The tuned-vs-RF advantage is borderline under the conservative non-parametric test ($p = 0.125$), so we cannot honestly claim that the tuned Chemprop ``wins'' against RF on these data.
  \item The Chemprop v2 release paper~\cite{chempropv2_2024} explicitly recommends the defaults as production-ready for moderate-size molecular property tasks; reporting both rows respects the convention while disclosing the asymmetric HPO outcome.
\end{itemize}

\textbf{Reproducibility.} The probe is reproducible from \texttt{tune\_chemprop.py} and the saved Optuna SQLite store \texttt{results/r1\_12\_chemprop\_optuna.db}; per-trial logs are in \texttt{logs/r1\_12\_chemprop\_trial\_*.log}. The 5-fold retrain is reproducible from \texttt{retrain\_chemprop\_tuned.py}, which calls \texttt{run\_chemprop.py} once per fold with the tuned hyperparameters injected via environment variables (\texttt{CHEMPROP\_D\_H}, \texttt{CHEMPROP\_DEPTH}, \texttt{CHEMPROP\_DROPOUT}, \texttt{CHEMPROP\_FFN\_N\_LAYERS}, \texttt{CHEMPROP\_FFN\_HIDDEN\_DIM}, \texttt{CHEMPROP\_BATCH\_SIZE}). Per-fold tuned-configuration artefacts land in \texttt{results/chemprop\_tuned\_v3\_fold\{0..4\}\_*.npy} alongside the default-configuration files; the aggregated paired-$t$ statistics are in \texttt{results/chemprop\_tuned\_v3\_cv\_aggregate.json}.}

\begin{changes}
  \item \textbf{Table~\ref{tab:model_comparison} (Model Comparison).} Added a new ``Chemprop (tuned, R1-12)$^{\ddagger}$'' row with RMSE = $28.83 \pm 1.32$, MAE = $13.19 \pm 2.16$, R$^2$ = $0.928 \pm 0.006$, Pearson = $0.963 \pm 0.003$. The default Chemprop row remains bolded as the primary comparator. Added a 5-line footnote ($^{\ddagger}$) explaining the budget-matched Optuna probe, the best configuration, the $p$-values vs.\ default ($t$-test $p = 0.074$, Wilcoxon $p = 0.125$) and vs.\ RF ($t$-test $p = 0.044$, Wilcoxon $p = 0.125$), the fold-4 outlier elimination, and the rationale for retaining defaults as the primary row.
  \item \textbf{Methods (D-MPNN subsection, R1-10 paragraph).} Expanded to cite Chemprop v2~\cite{chempropv2_2024} alongside the original Yang et al.\ paper, and appended two sentences describing the R1-12 Optuna probe, the 33.15 $\to$ 28.83~nm tuned improvement, the fold-4 outlier elimination, and the borderline RF advantage with both $p$-values reported.
  \item \textbf{Limitations (pg.\ 40).} Added a new bullet ``\textbf{Chemprop default hyperparameters are undertrained for this dataset}'' summarising the HPO finding: 5.6M-parameter tuned configuration improves CV RMSE by $4.32$~nm and reduces fold variance from 3.27 to 1.32, with the implication that production deployments should perform task-specific HPO rather than relying on the published defaults out of the box.
  \item \textbf{Supporting Information, new Section~S13.} Added ``Chemprop Hyperparameter Probe (R1-12)'' with the search space, best configuration, per-fold tuned-vs-default RMSE table, per-fold tuned-vs-RF table, chromophore-novel subset numbers, and reproducibility pointers to \texttt{tune\_chemprop.py}, the Optuna SQLite store, and \texttt{retrain\_chemprop\_tuned.py}.
  \item \textbf{Abstract, Conclusion bullet 1, and Introduction contributions bullet.} \emph{No change.} The bolded primary numbers in Table~\ref{tab:model_comparison} are unchanged, and the borderline tuned-vs-RF result does not warrant rewriting the headline statistical-tie language in the abstract/conclusion. The HPO finding is disclosed in Methods, the supplementary table row, Limitations, and SI Section~S13, which we judged sufficient transparency.
  \item \textbf{Bibliography (R1-19 piggyback).} Added \texttt{chempropv2\_2024} (Heid, Greenman, Chung, Li, Graff, Jorner, McGill, Glandorf, Vermeire, Stark, Yang, Coley, Green, \textit{J.\ Chem.\ Inf.\ Model.} \textbf{2024}, \textit{64}, 9--17, DOI 10.1021/acs.jcim.5c02332). Cited in the D-MPNN Methods paragraph, in the Table~\ref{tab:model_comparison} R1-12 footnote, and in SI Section~S13.
  \item \textbf{R1-2 response amended.} Replaced the previous ``variance is intrinsic to Chemprop on this dataset rather than protocol-driven'' clause with an explicit pointer to the R1-12 HPO finding, which demonstrates that the variance is in fact a consequence of undertrained defaults and falls away with appropriate HPO.
\end{changes}

\subsection*{Comment R1-19: Cite the Chemprop v2 paper}
\begin{reviewer}
Page 17 line 13: The Chemprop v2 paper (\url{https://doi.org/10.1021/acs.jcim.5c02332}) should be cited for the implementation that's used in this work.
\end{reviewer}

\response{We thank the reviewer for pointing this out and have added the Chemprop v2 release paper~\cite{chempropv2_2024} (Heid et al., \textit{J.\ Chem.\ Inf.\ Model.} \textbf{2024}, \textit{64}, 9--17, DOI 10.1021/acs.jcim.5c02332) to the bibliography and cited it in the D-MPNN Methods paragraph, in the Table~\ref{tab:model_comparison} R1-12 footnote (where it supports the rationale for retaining the published defaults as our headline row for apples-to-apples comparison with prior benchmarks), and in SI Section~S13. This change was bundled with the R1-12 manuscript edits, which is why the R1-19 entry is concise.}

\begin{changes}
  \item \textbf{Bibliography.} Added \texttt{chempropv2\_2024} (Heid, Greenman, Chung, Li, Graff, Jorner, McGill, Glandorf, Vermeire, Stark, Yang, Coley, Green, \textit{J.\ Chem.\ Inf.\ Model.} \textbf{2024}, \textit{64}, 9--17) to \texttt{Proposal.bib}.
  \item \textbf{Methods (D-MPNN subsection).} Added the Chemprop v2 citation alongside the original Yang et al.\ message-passing reference.
  \item \textbf{Supporting Information Section~S13 and Table~\ref{tab:model_comparison} footnote.} Added the Chemprop v2 citation in support of the rationale for retaining defaults as the primary row.
\end{changes}

\subsection*{Comment R1-13: Wetlab dataset scaffold/dye-family OOD check}
\begin{reviewer}
The claim that the wetlab validation set represents a ``genuine out-of-distribution test'' (pg.\ 32 line 24) is worth scrutinising. Are these 16 molecules genuinely outside the training distribution, or are they on chromophore scaffolds that are well-represented in the training data? A simple Tanimoto-similarity-to-nearest-training-neighbour audit (and a Bemis--Murcko scaffold-match check) would clarify how OOD the wetlab benchmark really is. If the wetlab compounds are on scaffolds well-represented in the training data, the framing should reflect that.
\end{reviewer}

\response{The reviewer is correct that the original ``15 entirely absent'' phrasing was over-broad. We performed exactly the audit the reviewer suggests: for each of the 16 wetlab compounds we computed (i)~nearest-neighbour Tanimoto similarity (Morgan fingerprints, radius~2, 2048~bits) against the full $7{,}846$-solute v3 cleaned Joung+Beard training set, and (ii)~exact-match check on the generic and specific Bemis--Murcko scaffolds. The complete per-molecule audit is in new Supporting Information Section~S14 and is reproducible from \texttt{analyze\_wetlab\_similarity.py}.

\textbf{Aggregate findings.}
\begin{itemize}[leftmargin=2em,itemsep=2pt]
  \item \emph{SMILES-level novelty.} Only Ferulic Acid is an exact duplicate of a training record (Tanimoto $= 1.000$). The other 15 molecules are absent from the training data and fingerprint-distant from it: nearest-train Tanimoto distribution mean~$= 0.58$, median~$= 0.58$, range $[0.350, 0.676]$. The four-bin breakdown is: $>0.85$ (near-duplicate) 1/16; $0.7$--$0.85$ (high) 0/16; $0.5$--$0.7$ (some) 10/16; $\leq 0.5$ (dissimilar) 5/16. The five fingerprint-novel molecules are Homosalate ($0.350$), Avobenzone ($0.379$), Amiloxate ($0.435$), Octocrylene ($0.458$), Octisalate ($0.491$).
  \item \emph{Scaffold-level overlap.} 15/16 generic Murcko scaffolds and 15/16 specific Murcko scaffolds are present in the v3 training set. Only Avobenzone (a 1,3-diketone bridging two substituted phenyl rings) has a generic scaffold absent from training.
\end{itemize}

\textbf{Interpretation: Case B (partially out-of-distribution).} The wetlab set is novel at the molecule level (substitution patterns, side chains, exact SMILES) but on chromophore scaffolds the models have seen at training time. We have therefore reframed the Experimental Validation section: rather than asserting full OOD, the wetlab benchmark tests \emph{molecule-level} (novel substitution-pattern) generalisation on familiar chromophore classes. This is the operational deployment scenario for the photosafety- and UV-filter-screening applications this manuscript targets (chemists screening new ester/ether substitutions on known UV-absorber scaffolds) and is a specifically-scoped but meaningful test. We are explicit that scaffold-class extrapolation requires either the Jung 2024 external benchmark (which has $\sim$50\% solute overlap with our primary training data and distinct experimental provenance, already in the manuscript) or a dedicated scaffold-split evaluation across the primary v3 dataset (flagged in the Limitations section as priority future work).

\textbf{Strict-5 subset analysis.} As an additional check requested in the reviewer's spirit, we re-computed the per-model wetlab MAE on the five Tanimoto-$\leq 0.5$ molecules (the truly fingerprint-novel ones) to test whether the DL-over-RF aggregate ranking is an artefact of the eleven scaffold-known compounds:

\begin{center}\small
\begin{tabular}{lccccc}
\toprule
\textbf{Subset} & \textbf{BiGRU (def)} & \textbf{BiGRU (tuned)} & \textbf{RF} & \textbf{ChemBERTa} & \textbf{Chemprop} \\
\midrule
All 16 (aggregate)                       & 28.64 & 31.52 & 38.46 & 26.29 & 26.18 \\
Scaffold-known 11 (Tan $\in (0.5, 1)$)   & 30.40 & 32.19 & 36.43 & 25.33 & 25.89 \\
\textbf{Strict-5 (Tan $\leq 0.5$)}       & \textbf{28.35} & 35.66 & \textbf{46.09} & 33.00 & 28.46 \\
\bottomrule
\end{tabular}
\end{center}

The DL-vs-RF gap actually \emph{widens} on the strict-5 subset (RF degrades $38.46 \to 46.09$~nm; BiGRU default and Chemprop both improve slightly; ChemBERTa degrades modestly). This strengthens rather than weakens the complementary-strengths narrative: the DL architectures retain their advantage over RF precisely on the molecules most chemically removed from training. The single most striking strict-5 result is Octocrylene, where RF over-predicts by $87.7$~nm (and Chemprop by $81.3$~nm) while BiGRU's error is $42.5$~nm; the molecule's $\alpha$-cyano-cinnamate substitution and long branched ester chain shift its absorption blue of what fingerprint-based similarity to other cinnamates would predict.

\textbf{Headline narrative impact.} The complementary-strengths narrative and the Table~\ref{tab:model_comparison}/abstract/conclusion bullet 1 statistical-tie language are unchanged: the qualifier shifts the \emph{interpretation} of what the wetlab set demonstrates (molecule-level rather than scaffold-class generalisation), but the wetlab still demonstrates DL retaining or widening its advantage over RF on novel substitution patterns, which is the operational scenario most relevant to deployment.}

\begin{changes}
  \item \textbf{Experimental Validation section (pg.\ 32).} Replaced the ``15 are entirely absent from the training dataset, providing a genuine out-of-distribution test'' / ``Ferulic Acid appears with a different solvent'' phrasing with a 5-sentence audit summary: at SMILES level, only Ferulic Acid is an exact duplicate (nearest-train Tanimoto mean $= 0.58$, 5/16 below $0.5$); at scaffold level, 15/16 generic Bemis--Murcko scaffolds are in training (only Avobenzone scaffold-novel); the wetlab set therefore tests molecule-level rather than scaffold-class generalisation; the strict Tanimoto-$\leq 0.5$ subset (in which the DL-vs-RF MAE gap widens) and the per-molecule audit are in SI Section~S14.
  \item \textbf{Limitations (pg.\ 40).} Added a new bullet ``\textbf{Wetlab benchmark tests molecule-level, not scaffold-class, generalisation}'' summarising the audit's headline numbers, noting that Jung 2024 is the closer approximation to a scaffold-class extrapolating test in the present manuscript, and flagging a dedicated scaffold-split evaluation as priority future work.
  \item \textbf{R1-9 response amendment.} Added a bracketed qualifier to the R1-9 entry's ``15 entirely absent'' phrasing pointing to SI Section~S14 and noting that the 15/16 share a Bemis--Murcko generic scaffold with training, so the wetlab tests molecule-level rather than scaffold-class generalisation.
  \item \textbf{Supporting Information, new Section~S14.} Added ``Wetlab Chemical-Space Audit (R1-13)'' with the four-bin Tanimoto histogram, the scaffold-match counts, the strict-5 subset MAE table, a Case-B interpretive paragraph, and a per-molecule audit table (16 rows: exact-duplicate flag, generic / specific Murcko scaffold-match flags, nearest-train Tanimoto, nearest-train mean $\lambda_{\max}$).
  \item \textbf{No abstract / conclusion bullet 1 / Table~\ref{tab:model_comparison} changes.} The headline numbers do not move because the audit qualifies the wetlab interpretation rather than altering it: DL still wins on wetlab MAE, RF still wins on cross-validated RMSE, and the cross-validated RF$\approx$Chemprop tie is unchanged.
  \item \textbf{Code release.} Added \texttt{analyze\_wetlab\_similarity.py} (Morgan-FP nearest-neighbour search + Murcko scaffold matching). Outputs land in \texttt{results/r1\_13\_wetlab\_per\_molecule.csv}, \texttt{results/r1\_13\_aggregate.json}, \texttt{results/r1\_13\_summary.md}, and \texttt{results/r1\_13\_subset\_mae.json}.
\end{changes}

\subsection*{Comment R1-14: Solvent ablation should be reported on multi-solvent subset only}
\begin{reviewer}
The reported $6$--$8$\% RMSE reduction from solvent encoding (Table~\ref{tab:solvent_ablation}) is a population-average figure across all test records. But chromophores that appear in the training data with only one solvent cannot teach the model a solvent-specific delta; the meaningful question is what the solvent-encoding gain is on chromophores that actually appear in multiple solvents in the training data. Re-reporting the ablation on that subset would clarify whether the claimed solvent-encoding contribution reflects a real learned correction or a population-averaged artefact.
\end{reviewer}

\response{The reviewer's intuition is correct, and the audit makes the contribution of solvent encoding substantially clearer than the aggregate number suggested.

\textbf{Method.} We re-computed the +Solvent vs.\ Solute-only paired ablation restricted to test records whose canonical solute SMILES appears in the corresponding (train+val) slice under $\geq 2$ distinct canonical solvent SMILES (the ``multi-solvent test subset''). The complement (``single-or-novel test subset'') is the set of test records whose chromophore either appears in train+val with only one solvent or does not appear at all, the regime where solvent encoding has no chromophore-specific signal to exploit. The same per-fold trained checkpoints used for Table~\ref{tab:solvent_ablation} are used here, so the only change is the subsetting; no new training was required. Across the five v2 folds, the multi-solvent subset comprises $59.7$--$61.2$\% of test records (mean $60.3$\%) and $|C^{\text{multi}}|$ ranges from $2{,}187$ to $2{,}208$ unique multi-solvent training chromophores (about $28$\% of the $\sim$$7{,}846$ unique training solutes).

\textbf{Results.}

\begin{center}\small
\begin{tabular}{lcccccccc}
\toprule
& \multicolumn{2}{c}{\textbf{Full (3{,}750)}} & \multicolumn{3}{c}{\textbf{Multi-solv ($\sim$2{,}260)}} & \multicolumn{3}{c}{\textbf{Single/novel ($\sim$1{,}490)}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-6} \cmidrule(lr){7-9}
\textbf{Model} & \textbf{+Solv} & \textbf{Solute} & \textbf{+Solv} & \textbf{Solute} & $\boldsymbol{\Delta}$ (\%) & \textbf{+Solv} & \textbf{Solute} & $\boldsymbol{\Delta}$ (\%) \\
\midrule
RF (tuned) & 31.34 & 34.13 & \textbf{15.49} & \textbf{22.72} & \textbf{7.22 (31.8\%)} & 45.85 & 46.27 & 0.42 (0.9\%) \\
Chemprop   & 31.69 & 33.78 & \textbf{17.55} & \textbf{23.46} & \textbf{5.91 (25.2\%)} & 45.28 & 45.00 & $-0.28$ ($-0.6$\%) \\
BiGRU      & 36.45 & 39.03 & \textbf{20.97} & \textbf{26.62} & \textbf{5.65 (21.2\%)} & 51.70 & 52.47 & 0.77 (1.5\%) \\
\bottomrule
\end{tabular}
\end{center}

\noindent
$\Delta = $ (Solute-only RMSE) $-$ (+Solvent RMSE) on that subset; positive means solvent encoding helps. All RMSEs in nm.

The aggregate $6$--$8$\% RMSE reduction reported in the original Table~\ref{tab:solvent_ablation} is the weighted average of (a) a $21$--$32$\% reduction on the $60$\% of test records whose chromophore appears in train under multiple solvents and (b) essentially zero ($\leq 1.5$\%) on the $40$\% of test records on single-solvent-or-novel chromophores where solvent encoding has no signal to exploit. The cross-architecture consistency is preserved on both subsets: RF $\geq$ Chemprop $>$ BiGRU on the multi-solvent gain, the same ranking as on the aggregate.

\textbf{Interpretation.} Solvent encoding can only contribute predictive value where solvent-specific signal exists in training. For a chromophore the model has seen in only one solvent, the model has no basis to attribute the prediction's variability to the solvent feature versus the solute feature; the solvent input becomes either an unused dimension or, at worst, an additional input the model has to learn to ignore (Chemprop's marginal $-0.28$~nm on the single-or-novel subset is consistent with this last interpretation). The multi-solvent subset is exactly the regime where the signal exists and the model can exploit it. The headline 21--32\% reduction matches the order-of-magnitude solvatochromic shifts reported in the photochemistry literature~\cite{reichardt2011solvents}, suggesting the architectures recover close to the maximum extractable solvent-specific signal on this subset.

A consequence relevant to deployment: practitioners screening compounds whose chromophore class is well-represented in their training data (with multiple solvent observations) should expect solvent-encoding gains of $\sim$20--30\%; those screening novel chromophore classes should not expect solvent encoding to contribute much beyond a generic regularisation effect. The aggregate $6$--$8$\% number is the right summary only for evaluation populations that resemble the cross-validated test mix.

\textbf{v2-vs-v3 caveat.} The analysis is performed on v2 first-occurrence-dedup splits because both ablation arms (+Solvent and Solute-only) were trained on v2; only the +Solvent arm was retrained on v3. The Greenman--Song cleaning removes records uniformly across chromophore-coverage strata (the v2-vs-v3 comparison in SI Section~\ref{si:v2_v3_comparison} confirms that the 1.81\% record loss is independent of multi-solvent membership), so the multi-solvent vs.\ single-or-novel ratios are expected to be preserved on v3.}

\begin{changes}
  \item \textbf{Abstract (line 55 of clean main).} Replaced ``a simple SMILES/fingerprint concatenation strategy yields 6--8\% RMSE reductions across fingerprint, graph, and recurrent architectures'' with the two-number framing ``reduces RMSE by 21--32\% on chromophores with multi-solvent training coverage (60\% of test records) and 6--8\% across the full benchmark.''
  \item \textbf{Results, Solvent Ablation paragraph (immediately after Table~\ref{tab:solvent_ablation}).} Replaced the ``consistency of the improvement'' sentence with the two-sentence amplification: ``Encoding solvent identity yields a strong but conditional improvement. On the 60\% of test records where the chromophore has been seen in training under multiple solvents, solvent encoding reduces RMSE by 21\% (BiGRU) to 32\% (RF) across the three architecture families; the contribution drops to $<1$\% on the 40\% of test records on chromophores absent or single-solvent in training, where solvent encoding has no signal to exploit. The aggregate 6--8\% reduction reported in Table~\ref{tab:solvent_ablation} is the weighted average of these two regimes (full per-model decomposition in SI Section~S15).''
  \item \textbf{Conclusion bullet 2 (``Solvent encoding improves all model families'').} Replaced ``reduces RMSE by 6--8\% across all tested architectures (RF 8\%, Chemprop 6\%, BiGRU 7\%; all $p < 0.02$)'' with ``reduces RMSE by 21--32\% on multi-solvent-covered chromophores and 6--8\% across the full benchmark ($p < 0.02$), with the contribution concentrated where solvent-specific training data exists.''
  \item \textbf{Supporting Information, new Section~S15 ``Solvent-Encoding Gain by Chromophore Multi-Solvent Coverage (R1-14).''} Added the subset definitions (multi-solvent / single-or-novel), the per-model decomposition table (RF / Chemprop / BiGRU full vs.\ multi vs.\ single subset RMSEs and $\Delta$s), a three-point discussion of why the multi-solvent subset is the regime where solvent encoding has signal to exploit, and a v2-vs-v3 caveat footnote with a pointer to Section~\ref{si:v2_v3_comparison}.
  \item \textbf{R1-9 response.} \emph{No change.} The chromophore-overlap framing in R1-9 already covers the structural cause of the multi-vs-single regime gap (the same chromophore-overlap that inflates Table~\ref{tab:model_comparison}'s in-distribution numbers also creates the multi-solvent regime where solvent encoding has signal), so R1-14 stands on its own without amendments to R1-9.
  \item \textbf{Limitations section.} \emph{No new bullet.} A separate Limitations bullet on this topic would overlap with the existing R1-9 chromophore-overlap framing; the SI Section~S15 cross-reference from Results provides the additional context without cluttering the Limitations.
  \item \textbf{Code release.} Added \texttt{analyze\_multisolvent\_subset.py}; per-fold and aggregate outputs land in \texttt{results/r1\_14\_multisolvent\_subset.json} and the human-readable \texttt{results/r1\_14\_summary.md}.
\end{changes}

\subsection*{Comment R1-17: ``Optical properties'' misclassification of lipophilicity}
\begin{reviewer}
Pg.\ 40 (Future Work): lipophilicity is listed alongside fluorescence emission and molar extinction coefficient as a ``related molecular optical property,'' but lipophilicity is a partition-coefficient property, not an optical one. This should be corrected.
\end{reviewer}

\response{The reviewer is correct. We have restructured the Future Work sentence to distinguish optical-property comparators (fluorescence emission wavelength, molar extinction coefficient $\varepsilon$) from non-optical comparators whose structure--property relationships are believed to be more globally determined (lipophilicity, aqueous solubility). The latter category is exactly the right target for testing the locality-of-property hypothesis stated earlier in the same paragraph, so lipophilicity remains in the sentence but is now framed correctly as a cross-property-family rather than within-optical comparator.}

\begin{changes}
  \item \textbf{Future Work paragraph (final sentence, pg.\ 40).} Replaced ``A natural extension is to apply the same benchmarking framework to related molecular optical properties, including fluorescence emission wavelength, molar extinction coefficient ($\varepsilon$), and lipophilicity, to determine whether the patterns observed here generalize across property types\ldots'' with ``A natural extension is to apply the same benchmarking framework to additional molecular properties, both within the optical-property family (fluorescence emission wavelength, molar extinction coefficient $\varepsilon$) and outside it on properties whose structure--property relationships are believed to be more globally determined (lipophilicity, aqueous solubility), to test whether the locality-of-property hypothesis stated above shifts the balance between architecture families in the predicted direction.''
\end{changes}

\subsection*{Comment R1-18: Code public during peer review}
\begin{reviewer}
The Data and Code Availability section states that code will be released upon publication. Standard practice in the field is for code to be publicly accessible during peer review so reviewers can verify the reported analyses. The authors should make the repository public now rather than upon publication.
\end{reviewer}

\response{We agree. The code repository has been publicly accessible at \url{https://github.com/Umesh1608/Paper-1_New} throughout the peer review process. The Data and Code Availability section previously stated ``will be released on GitHub under the MIT license upon publication,'' which reflected the standard pre-submission availability statement; we have updated this to indicate the repository is currently public, with all training scripts, per-fold metrics, configuration JSONs, and the analyses associated with this revision available under the MIT license. Trained model weights (.keras, .ckpt, .bin) are gitignored due to size but are available on request and will be uploaded to a Zenodo archive associated with the published version of the manuscript.}

\begin{changes}
  \item \textbf{Data and Code Availability section (pg.\ 41).} Replaced the future-tense statement (``will be released on GitHub under the MIT license upon publication, including trained model weights and preprocessing scripts'') with the present-tense statement (``is publicly available at \url{https://github.com/Umesh1608/Paper-1_New} under the MIT license, including trained model weights, preprocessing scripts, and the analyses associated with this revision (e.g., \texttt{analyze\_nonlocal\_subsets.py}, \texttt{analyze\_deep4chem\_overlap.py}, \texttt{analyze\_chromophore\_leakage.py}, \texttt{analyze\_duplicate\_handling.py}, \texttt{analyze\_wetlab\_similarity.py}, \texttt{tune\_chemprop.py}, and the Greenman--Song duplicate-handling script). The repository has been publicly accessible throughout peer review''). Added \texttt{\textbackslash usepackage\{url\}} to the preamble to support the \texttt{\textbackslash url} command (achemso does not load it by default).
\end{changes}

\subsection*{Comment R1-15: Mamede/Reaxys dataset reproducibility}
\begin{reviewer}
The Photosafety Classification section briefly describes a reconstruction of the Mamede et al.\ dataset by re-querying Reaxys with the original study's CAS registry numbers, with a $93.6$\% recovery rate. This is too thin for reproducibility purposes. What was the actual query (search fields, filters, export settings)? When was it run (Reaxys changes over time)? How were structures canonicalised? What was the train/test split protocol? Without these details, no independent reader can reproduce the photosafety classifier reported in Table~\ref{tab:classification}.
\end{reviewer}

\response{We thank the reviewer for prompting a careful audit of our reproducibility procedure. In preparing this revision, we discovered that the original manuscript's claim that we ``reconstructed the Mamede/Reaxys dataset by querying Reaxys using the CAS registry numbers listed in the original study, recovering $93.6$\%'' was incorrect: the actual reproducibility code reads Mamede et al.'s publicly available Supporting Information (a Reaxys-derived XRN+SMILES list distributed with the \emph{Scientific Reports} article) and canonicalises it through RDKit. The canonicalisation succeeds for $74{,}783$ of the $74{,}784$ SMILES strings in Mamede's SI ($\sim$100\% recovery; one SMILES fails to parse). This procedure is in fact more reproducible than a fresh Reaxys query, because Mamede's SI is publicly accessible without an institutional Reaxys licence; anyone can re-run the pipeline from the SI download forward. We have corrected the manuscript text in Section~\ref{sec:classification} (Photosafety Classification) and added a full reproducibility pipeline (SI download URL (DOI 10.1038/s41598-021-83104-4), RDKit version (\texttt{>= 2024.03}), canonicalisation script (\texttt{postprocess\_reaxys.py}), training script (\texttt{run\_mamede.py rf\_cls}), and the seed-7 stratified train/test split protocol ($72{,}787$ train / $998$ \texttt{test\_I} / $998$ \texttt{test\_II})) to Supporting Information Section~S16. The classification model (RandomForestClassifier, $n_{\text{estimators}} = 500$, Morgan fingerprints of radius $2$ and $1024$ bits) and its reported performance (sensitivity $= 0.876$, specificity $= 0.882$, accuracy $= 0.879$, AUC $= 0.942$) are unchanged because the underlying dataset and training pipeline have always been what is now documented in SI Section~S16; only the manuscript wording was incorrect.

This is the second factual correction surfaced by the revision process, the first being the R1-12-driven correction in the R1-2 response to the claim that Chemprop's high default variance is ``intrinsic to Chemprop on this dataset'', the R1-12 HPO probe demonstrated the variance falls away with appropriate hyperparameter tuning. We list both corrections in the Summary of Major Changes for transparency.}

\begin{changes}
  \item \textbf{Section~\ref{sec:classification} (Photosafety Classification, pg.\ 33).} Replaced ``We reconstructed the Mamede/Reaxys dataset by querying Reaxys using the CAS registry numbers listed in the original study, recovering 93.6\% of the original 74,784 compounds. The 6.4\% gap arises from deprecated CAS entries\ldots'' with the corrected procedure: the dataset comes from Mamede's published SI (a Reaxys-derived XRN+SMILES table), RDKit canonicalisation succeeds for $74{,}783$ of $74{,}784$ entries ($\sim$100\% recovery), and full pipeline detail is in SI Section~S16.
  \item \textbf{Supporting Information, new Section~S16 ``Mamede Photosafety Dataset Reproducibility (R1-15).''} Added (a) upstream source citation with DOI, (b) the five-step local pipeline (download, parse + canonicalise, label join, train/test split, model), (c) software versions (RDKit $\geq 2024.03$, scikit-learn 1.1+, seed = 7), (d) output artefacts (\texttt{results/mamede\_rf\_cls\_metrics.json}), (e) reproducibility command (two-line shell snippet), and (f) an explicit ``Correction to original manuscript'' paragraph noting the corrected provenance and that the model and metrics are unchanged.
  \item \textbf{Public repository.} The pipeline scripts (\texttt{postprocess\_reaxys.py}, \texttt{run\_mamede.py}), the upstream Mamede SI table (\texttt{data/raw/mamede\_supplementary.txt}), and the resulting per-fold metrics JSON (\texttt{results/mamede\_rf\_cls\_metrics.json}) are all in the public repository linked in the Data and Code Availability section.
  \item \textbf{No model or metric changes.} The reported Table~\ref{tab:classification} numbers (sensitivity, specificity, accuracy, AUC) are unchanged because the underlying RF training has always used the corrected SI-derived dataset; only the manuscript description was wrong.
\end{changes}

\section*{Reviewer 2}

\subsection*{Comment R2-3: MoleculeNet inclusion plans for the cleaned dataset}
\begin{reviewer}
Are there plans to include this benchmark, or a cleaned version of it, in MoleculeNet or a similar standardized benchmark suite? This would make future comparisons with additional models easier and more reproducible.
\end{reviewer}

\response{Yes. Our intent is twofold: (a) we have made the v3 Greenman--Song-cleaned primary dataset (the $18{,}415$-record CSV produced by the duplicate-handling protocol from R1-8) immediately available as a single file in the public GitHub repository together with the per-fold stratified CV indices, so independent groups can reproduce our Table~\ref{tab:model_comparison} numbers exactly without re-running the cleaning pipeline; (b) we will create a permanent Zenodo deposit of the v3 dataset prior to publication so the cleaned benchmark has a citable DOI independent of the GitHub repository, and we plan to submit a contribution PR proposing the v3 dataset for inclusion in MoleculeNet (which currently does not contain any UV absorption benchmark, as already noted in the Related Work ``Benchmarking perspective'' paragraph). The MoleculeNet contribution PR is the right vehicle for ensuring future cross-architecture comparisons on UV absorption use the same dataset, splits, and evaluation protocol, which addresses the reviewer's reproducibility goal directly. We have added a sentence to the Data and Code Availability section describing this plan.}

\begin{changes}
  \item \textbf{Data and Code Availability section (pg.\ 41).} Added two sentences immediately after the Mamede dataset description: the v3 cleaned dataset CSV and per-fold CV indices are now explicitly named as artefacts in the public repository; and the planned Zenodo deposit + MoleculeNet contribution PR are noted as forthcoming for the published version.
  \item \textbf{Related Work / Benchmarking perspective paragraph.} \emph{No change.} The existing sentence ``MoleculeNet established a standardized framework for comparing molecular ML models across diverse property prediction tasks, but UV absorption is not among its benchmarks'' already provides the right anchor for our deposit-and-PR plan; expanding it further would be redundant with the Data and Code Availability statement.
  \item \textbf{Future Work section.} \emph{No new sentence.} The Data and Code Availability statement carries the operational commitment; the Future Work section already discusses cross-property benchmarking which would naturally flow from a standardised v3 deposit.
\end{changes}

\subsection*{Comment R2-8: LLM / general-purpose foundation-model extensions}
\begin{reviewer}
Could this work be extended to modern large language models or general-purpose molecular foundation models? For example, it would be interesting to discuss whether GPT-like or Claude/Opus-style models could contribute through SMILES featurization, chemical reasoning, data curation, or as part of an agentic workflow, even if they are not directly used as regression models.
\end{reviewer}

\response{We agree that this is a natural extension, and we have added a dedicated Future Work paragraph that flags three mechanisms by which LLMs and general-purpose molecular foundation models could contribute to this benchmark family even when they are not used directly as regression heads: (i) SMILES augmentation or canonicalisation under multiple equivalent representations, (ii) data curation tasks such as de-duplication, solvent-name standardisation, and outlier detection from natural-language abstracts, and (iii) agentic workflows that hand off literature retrieval, retrosynthesis, and TD-DFT calculation to specialised tools. We name MoLFormer~\cite{ross2022molformer} and the recent Llama/Mistral chemistry derivatives as concrete examples, alongside GPT-style and Claude-style decoders. The Future Work paragraph is intentionally directional rather than predictive: the present manuscript does not benchmark these models, and the literature on LLM-based molecular property prediction is still rapidly evolving, so we frame the contribution as a research agenda rather than an empirical claim.}

\begin{changes}
  \item \textbf{Future Work section (after the cross-property paragraph).} Added a new paragraph titled ``Two further model families'' containing two sentences: one on pretrained molecular graph foundation models (R2-9, see below), and one on LLMs and general-purpose foundation models with the three contribution mechanisms above. The MoLFormer citation is the concrete grounding for the foundation-model claim.
  \item \textbf{Bibliography.} Added \texttt{ross2022molformer} (Ross et al., \textit{Nat.\ Mach.\ Intell.} \textbf{2022}, \textit{4}, 1256--1264) to \texttt{Proposal.bib}.
\end{changes}

\subsection*{Comment R2-9: Pretrained molecular GNN foundation models}
\begin{reviewer}
Are there pretrained or foundational graph neural network models for molecules that could be adapted to this task by adding a new readout head for UV absorption prediction? This might provide a useful comparison to ChemBERTa as the pretrained sequence-model baseline. This point is also related to point 7.
\end{reviewer}

\response{Yes, and we have added a dedicated Future Work paragraph naming concrete pretrained graph models: GROVER~\cite{rong2020grover}, MolCLR~\cite{wang2022molecular}, GraphMVP~\cite{liu2021pretraining}, Uni-Mol~\cite{zhou2023unimol}, and the recent Chemprop-foundation variants from the Chemprop v2 release line. The motivation for the paragraph is exactly what the reviewer suggests: these models would isolate whether a graph (rather than sequence) pretrained representation can match ChemBERTa's wetlab MAE while preserving Chemprop's locality-aligned inductive bias, i.e., whether the pretrained-vs.-from-scratch axis dominates the architecture axis, or whether the architecture (sequence vs.\ graph) matters even when both are pretrained. The connection to R2-7 (D-MPNN parameter budget) is noted in the paragraph because both questions probe whether the present comparison's headline ranking changes under scale or pretraining shifts. The paragraph stops short of running these benchmarks in the current revision because each adds significant compute and the right comparison would also require matched per-architecture HPO (as we did for Chemprop in R1-12), which exceeds the revision scope.}

\begin{changes}
  \item \textbf{Future Work section.} The new ``Two further model families'' paragraph (added for R2-8) leads with pretrained molecular graph foundation models, naming GROVER, MolCLR, GraphMVP, Uni-Mol, and Chemprop-foundation variants, and explicitly states the contrast with ChemBERTa as the question this benchmark line would resolve.
  \item \textbf{Bibliography.} Added \texttt{rong2020grover}, \texttt{wang2022molecular}, \texttt{liu2021pretraining}, \texttt{zhou2023unimol} to \texttt{Proposal.bib}.
\end{changes}

\subsection*{Comment R2-10: Figure~5 axis limits extend into unphysical territory}
\begin{reviewer}
In Figure~5, the lower limits of the x- and y-axes appear to extend to approximately $-200$~nm. Since the physically relevant and observed $\lambda_{\max}$ values are positive and much closer to the 200--900~nm range, would it be more informative to set the axis limits closer to the data range?
\end{reviewer}

\response{The reviewer is correct. The previous Figure~\ref{fig:parity_5models} was generated with axis limits computed as \texttt{min(y\_true.min(), y\_pred.min()) - 10} to \texttt{max(\ldots) + 10}, which let outlier predictions from ChemBERTa (whose prediction ceiling produces a small cluster of negative values when input molecules drift far from the training distribution) drag the lower bound below zero. We have updated the parity-plot generator (\texttt{make\_parity\_plot.py}) to clamp the lower bound to \texttt{max(150, y\_true.min() - 20)} (computed from the \emph{experimental} $\lambda_{\max}$ distribution only, which is physical) and regenerated the figure. The new figure (a) brings the axes into the $\sim$150--$\sim$1100~nm physical UV/Vis range, (b) preserves the visual diagnostic of ChemBERTa's prediction ceiling around 600~nm (now visible as a horizontal band of predictions at $\sim$600~nm against the experimental range $\sim$200--1000~nm rather than against an unphysical $-200$~nm floor), and (c) does not change any reported quantitative metric, since the change is purely visual.}

\begin{changes}
  \item \textbf{Figure~\ref{fig:parity_5models} (\texttt{results/parity\_plot\_5models.png}).} Regenerated with axis limits clamped to the physical experimental $\lambda_{\max}$ range. No caption change because the caption already describes the parity plot generically (RMSE/MAE/$R^2$ annotations and the $\pm 20$~nm band are unchanged).
  \item \textbf{Code.} The fix is a 5-line change in \texttt{make\_parity\_plot.py} (replacing the prediction-inclusive \texttt{global\_min}/\texttt{global\_max} computation with an experimental-$\lambda_{\max}$-only computation clamped at 150~nm). The change is in the public repository.
\end{changes}

\subsection*{Comment R2-7: D-MPNN 636K parameter count justification for the dataset size}
\begin{reviewer}
The D-MPNN configuration reports 636K trainable parameters but the primary dataset is only $\sim$18K samples (and the per-fold train slice is $\sim$12K). Is this parameter budget appropriate for the training data available? What overfitting controls are in place?
\end{reviewer}

\response{We agree this is worth being explicit about, and have expanded the D-MPNN Methods paragraph with three sentences that (a) report the parameter-to-sample ratio in context ($\sim$54 parameters per training example, computed against the $\sim$11{,}780-record per-fold train slice; higher than the $\sim$10--20 typical of from-scratch QSPR models but well below the $\geq 10^3$ characteristic of pretrained transfer-learning regimes), (b) enumerate the three regularisation mechanisms Chemprop uses in lieu of explicit parameter reduction (Noam learning-rate decay, early stopping on val loss with patience 15, and default dropout of 0.0 deliberately disabled because the Chemprop v2 authors found target-normalisation plus early stopping sufficient on the datasets it was tuned for~\cite{chempropv2_2024}), and (c) note that our R1-12 HPO probe confirmed the defaults are conservative but not pathological at this dataset size (adding modest dropout $= 0.1$ improves RMSE by $\sim$4~nm). The Chemprop v2 release paper validates this parameter regime across moderate-size molecular property datasets, so our default-config choice is consistent with the published-benchmark convention rather than an inadvertent overparameterisation.}

\begin{changes}
  \item \textbf{Methods D-MPNN subsection (pg.\ 17).} Inserted three sentences after the existing ``Target values are normalised\ldots'' line, reporting (i) parameter-to-sample ratio with context for from-scratch vs.\ transfer-learning regimes, (ii) the three regularisation mechanisms (Noam decay, early stopping with patience 15, default dropout $= 0$), and (iii) a forward pointer to the R1-12 HPO finding that adding dropout $= 0.1$ improves RMSE by $\sim$4~nm. Citation: \texttt{chempropv2\_2024} (already in the bibliography from R1-19).
\end{changes}

\subsection*{Comment R2-2: Joung+Beard dataset overview figure}
\begin{reviewer}
A visual overview of the primary dataset---the distribution of $\lambda_{\max}$ values, the solvent diversity, and representative chromophore structures---would help readers calibrate the scope and class composition of the benchmark.
\end{reviewer}

\response{We agree, and have added a three-panel dataset overview figure as new Supporting Information Figure~S1: (a) the $\lambda_{\max}$ histogram across the $18{,}415$ records (median $= 396$~nm, range $[162, 1100]$~nm, with the bulk in the UV/early-visible region 250--500~nm and a long tail of deep-coloured chromophores extending to the near-IR), (b) the top-20 solvents by record count on a log scale (DCM, MeCN, CHCl$_3$, toluene, THF, MeOH, EtOH, \ldots; note the long tail of $\sim$700 rarely-used solvents not shown), and (c) eight representative chromophore structures spanning the major class diversity (hydroxycinnamic acids, linear acenes, push--pull aromatics, azo dyes, BF$_2$-dipyrromethene BODIPYs, push--pull coumarins, extended-quinoid indigo, and porphyrin macrocycles). The figure also helps motivate the non-local subset analysis (Section~5.3): both molecules dominated by local conjugation (hydroxycinnamic acids, BODIPYs) and molecules where extended conjugation and donor--acceptor charge transfer span the entire scaffold (porphyrins, indigo, push--pull cyanines) are represented. We placed this figure in the SI rather than the main text to avoid bloating the main text after R1-10 just compressed it; a forward pointer is added to the Methods Dataset Description paragraph.}

\begin{changes}
 \item \textbf{Supporting Information (new Section ``Dataset Overview (R2-2)'' as Section~S1.} Added the three-panel figure plus a paragraph describing each panel. Insertion at the top of the SI (between the title block and the existing Section~S1 ``Per-Fold Cross-Validation Results,'' which becomes Section~S2 by document order) no $\backslash$ref's break because the existing section uses $\backslash$label{sec:perfold} and there are no cross-references to it by hardcoded number).
  \item \textbf{Methods Dataset Description (pg.\ 9).} Added one sentence at the end of the ``After merging and applying the duplicate-handling protocol\ldots 18{,}415 solute--solvent pairs\ldots'' paragraph, pointing to SI Figure~S1.
  \item \textbf{Figure renumbering propagated.} Inserting the new figure at the top of the SI bumps the existing four SI figures' numbers each by one: BiGRU learning curves S1$\to$S2; ChemBERTa curves S2$\to$S3; Deep4Chem overlap Venn S3$\to$S4; BiGRU character-type saliency S4$\to$S5. The four hardcoded main-text references (lines 294, 897, 902, 1110 of the clean main) were updated accordingly; the marked main uses the existing $\backslash$removed/$\backslash$added wrappers from R1-7 to show the chain of renumbers.
  \item \textbf{Code release.} Added \texttt{make\_dataset\_overview.py} (RDKit \texttt{MolsToGridImage} for panel (c); matplotlib for panels (a)/(b); solvent-name dictionary in-script). Output: \texttt{results/figure\_dataset\_overview.png}.
\end{changes}

\subsection*{Comment R2-6: Add D-MPNN to Figure~\ref{fig:model_overview} architecture comparison}
\begin{reviewer}
Figure~2 currently compares three architecture families (RF, BiGRU, ChemBERTa). The D-MPNN is the fourth headline model in the manuscript and should be visualised alongside the others so readers can compare its architecture at a glance.
\end{reviewer}

\response{Agreed. We have added a fourth panel for the Chemprop D-MPNN to Figure~\ref{fig:model_overview}, immediately after the Random Forest panel (so the figure now reads RF $\to$ D-MPNN $\to$ BiGRU $\to$ ChemBERTa, roughly in order of increasing parameter count: 636K $\to$ 3.0M $\to$ 44.1M). The new panel uses the same TikZ \texttt{archbox} style as the other three and shows the eight-stage pipeline: solute + solvent SMILES $\to$ RDKit graph construction $\to$ two parallel D-MPNN encoders (each $T = 3$ message-passing steps, $d_h = 300$) for solute and solvent respectively $\to$ mean-pool + concatenation $\to$ feed-forward head $\to$ linear scalar output. The figure caption was extended to describe the new panel and to note the parallel-encoder multicomponent configuration as the D-MPNN's mechanism for incorporating solvent information (distinct from the SMILES concatenation used by the other three). The layout switched from three minipages at 0.30 textwidth to four minipages at 0.23 textwidth; the font size of the per-panel header was reduced from \texttt{\textbackslash small} to \texttt{\textbackslash scriptsize} to keep the panels visually balanced. XGBoost remains unillustrated (it shares the RF pipeline with a gradient-boosted tree ensemble in place of the RF) and is now mentioned in the caption.}

\begin{changes}
  \item \textbf{Figure~\ref{fig:model_overview} TikZ block (pg.\ 12).} Restructured from three panels (RF, BiGRU, ChemBERTa) to four panels (RF, D-MPNN, BiGRU, ChemBERTa) at 0.23 textwidth each. New \texttt{archboxnarrow} TikZ style added to fit narrower minipages.
  \item \textbf{Figure caption.} Updated ``three model families'' $\to$ ``four model families,'' added a sentence describing the D-MPNN panel and the parallel-encoder multicomponent configuration, and added a sentence noting XGBoost shares the RF pipeline.
  \item \textbf{Introduction-to-Figure sentence (Methods opener at pg.\ 12).} ``three model families'' $\to$ ``four model families.''
\end{changes}

\subsection*{Comment R2-1: Experimental $\lambda_{\max}$ measurement uncertainty}
\begin{reviewer}
The reported model errors (RMSE in the $31$--$54$~nm range) should be contextualised against the inherent measurement uncertainty in the experimental $\lambda_{\max}$ values themselves. What is the typical inter-laboratory variability for UV/Vis absorption measurements? How much of the model error is data noise versus genuine model limitation?
\end{reviewer}

\response{We have quantified this on the v2 first-occurrence-dedup dataset (which retains the full pre-cleaning duplicate distribution; the v3 Greenman--Song cleaning removes high-spread duplicates by design and would understate the noise floor). Two complementary measurements:

\textbf{(a) Within-source duplicate spread (n~=~294 multi-record keys).} Of the $18{,}755$ v2 records, $294$ (canon, solvent) keys appear with multiplicity~$> 1$. The within-key spread distribution is heavily right-skewed: median~$= 0$~nm (most duplicates are perfectly-agreeing re-extractions of the same primary measurement across multiple secondary citations); $86$\% of multi-record keys have spread~$\leq 5$~nm (consistent with typical UV-Vis instrument precision of $1$--$5$~nm); $95$th percentile = $38$~nm; and $4.1$\% have spread~$> 50$~nm (large disagreements likely attributable to lab-to-lab protocol differences or labelling errors). The Greenman--Song 5-nm cleaning rule discards the latter tail, this is the rationale for the R1-8 cleaning, and the audit confirms the threshold is sensibly placed at the boundary between instrument-precision-consistent and protocol-difference-consistent regimes.

\textbf{(b) Inter-source disagreement (Joung vs.\ Beard, n~=~21 paired keys).} For the $21$ (canon, solvent) keys reported in both Joung 2020 and Beard 2019, the median $|\Delta\lambda_{\max}| = 49$~nm (RMSD~$= 48$~nm; p95~$= 76$~nm); only $38$\% of paired observations agree within $5$~nm. We treat this as an upper bound on inter-laboratory disagreement rather than a representative statistic, because the $21$ paired compounds are biased toward chromophores of multi-paper solvatochromic interest, which are exactly the molecules most sensitive to small differences in solvent purity, temperature, and concentration. The small $n$ also limits statistical power.

\textbf{Implication for model error.} The headline RF~$\approx$~Chemprop tie ($31.5$~nm vs.\ $33.2$~nm, $p = 0.16$) and the broader $31$--$54$~nm range of model RMSEs are comparable to the $95$th percentile of the within-source spread distribution ($38$~nm in v2-combined). On the chromophores where the underlying measurements agree perfectly, model error is largely model-attributable; on the chromophores in the $\sim$10\%-tail of high disagreement, model error includes a substantial data-noise component. The diminishing-returns regime for architectural improvement on this dataset is therefore at $\sim$30~nm RMSE, which is where RF and Chemprop currently sit. Closing the residual gap requires either improved data curation at the source or models that explicitly quantify prediction uncertainty alongside the point estimate.

We have added a brief paragraph to the Methods Dataset Description section reporting the within-source and inter-source numbers, and a full SI Section~S17 with the per-source decomposition and reproducibility pipeline.}

\begin{changes}
  \item \textbf{Methods Dataset Description (pg.\ 9).} Added a new paragraph titled ``Experimental measurement uncertainty'' reporting the within-source numbers ($n = 294$ multi-record keys, median spread $= 0$~nm, $86$\% within $5$~nm, p95 $= 38$~nm) and the inter-source numbers ($n = 21$ Joung--Beard pairs, median $|\Delta| = 49$~nm) with the caveat about the small inter-source $n$ and its biased composition. Forward pointer to SI Section~S17.
  \item \textbf{Supporting Information, new Section~S17 ``Experimental $\lambda_{\max}$ Uncertainty Audit (R2-1).''} Contains (a) within-source spread table by source (Joung / Beard / v2 combined), (b) inter-source disagreement statistics, (c) discussion of the implication for the model-error budget, and (d) reproducibility command pointing to \texttt{analyze\_experimental\_uncertainty.py}.
  \item \textbf{Code release.} Added \texttt{analyze\_experimental\_uncertainty.py}; outputs land in \texttt{results/r2\_1\_uncertainty.json} and \texttt{results/r2\_1\_uncertainty\_summary.md}.
\end{changes}

\subsection*{Comment R2-4: Solvatochromism analysis (per-solute solvent shifts)}
\begin{reviewer}
The manuscript would benefit from a direct empirical characterisation of the magnitude of solvatochromism in the primary dataset. For chromophores measured under multiple solvents, what is the typical $\lambda_{\max}$ span? Which chromophore classes show the largest solvent-induced shifts?
\end{reviewer}

\response{We have added a per-chromophore solvatochromism audit (new Supporting Information Section~S18, summarised in the Solvent Information Encoding subsection of the main text). For each chromophore appearing under $\geq 2$ canonical solvents in the v3 dataset ($2{,}516$ chromophores ($32$\% of the $7{,}846$ unique solutes)) we computed the per-chromophore span $\max(\lambda_{\max}) - \min(\lambda_{\max})$. The headline distribution is heavily right-skewed: median $= 10$~nm (consistent with the literature's $5$--$20$~nm typical solvatochromic shift for non-charge-transfer chromophores), $95$th percentile $= 52$~nm, $99$th percentile $= 123$~nm, max $= 494$~nm (Reichardt's $E_T(30)$ betaine). The top-decile by span is dominated by classical solvatochromism standards and the structural classes the literature predicts (push--pull merocyanines, stilbene-pyridinium cyanines, and zwitterionic dyes) confirming that the v3 dataset captures the full operational range of solvatochromic behaviour relevant for evaluating UV/Vis prediction models. A histogram of the per-chromophore span distribution and a top-15 table with structural class flags are in SI Section~S18.}

\begin{changes}
  \item \textbf{Methods Solvent Information Encoding subsection (pg.\ 21).} Added a sentence at the start of the paragraph reporting the $2{,}516$ multi-solvent chromophores, median span $10$~nm, $95$th percentile $52$~nm, and the top-decile dye-class observation ($E_T(30)$, merocyanines, push--pull cyanines). Forward pointer to SI Section~S18.
  \item \textbf{Supporting Information, new Section~S18 ``Per-Solute Solvent-Induced Shifts (R2-4).''} Contains the full per-chromophore span distribution histogram (Figure~\ref{fig:si_solvent_shift_hist}), summary statistics in $7$ percentile bins, and a $15$-row top-spans table with structural class annotations. Cross-reference to the upcoming polarity-grouped analysis (R2-5).
  \item \textbf{Code release.} Added \texttt{analyze\_solvent\_shifts.py}; outputs land in \texttt{results/r2\_4\_solvent\_shifts.\{json,csv,png\}} and \texttt{results/r2\_4\_summary.md}.
\end{changes}

\subsection*{Comment R2-5: Polarity-grouped prediction variability}
\begin{reviewer}
Beyond the per-chromophore solvatochromism audit (R2-4), it would be useful to know whether prediction quality depends systematically on the polarity class of the solvent. Are models worse on polar protic solvents (where hydrogen bonding dominates) than on nonpolar solvents? A by-tier RMSE breakdown would clarify this.
\end{reviewer}

\response{We have added the per-(model, polarity-tier) breakdown as new Supporting Information Section~S19, using the standard four-tier $E_T(30)$-style classification (nonpolar / moderate / polar aprotic / polar protic, plus an ``other'' bucket for long-tail solvents not represented in the standard tier definitions). The headline finding is a null result: \emph{no polarity tier is dramatically worse across all models}, and the relative ranking (RF $\lesssim$ Chemprop $<$ BiGRU $\ll$ ChemBERTa) is preserved on every tier. Two specific patterns are noteworthy: (a) all four models perform best on nonpolar solvents (RF $= 27.7$~nm, Chemprop $= 28.4$~nm, vs.\ ALL $= 31.5$ and $33.3$~nm respectively), consistent with the solubility-constraint observation that the nonpolar tier is enriched in less complex chromophores; (b) ChemBERTa's degradation is concentrated on the ``other'' tier ($84.4$~nm vs.\ $54.2$~nm aggregate), this is the same prediction-ceiling artefact diagnosed elsewhere in the manuscript (R1-16). Because no tier reveals a systematic relative-ranking shift, we placed the analysis in the SI only; no main-text edit is needed.}

\begin{changes}
  \item \textbf{Supporting Information, new Section~S19 ``Solvent Polarity Stratification (R2-5).''} Contains tier definitions, the per-(model, tier) RMSE table for the four headline models on the full v3 test pool, and a three-point discussion of the cross-architecture-consistency, nonpolar-easiest, and ChemBERTa-fails-on-``other'' findings. Cross-references the R1-16 ChemBERTa-ceiling section forward.
  \item \textbf{Main text.} \emph{No change.} The polarity-grouped analysis does not change the headline cross-architecture ranking or the statistical-tie claim, so it stays in the SI.
  \item \textbf{Code release.} Added \texttt{analyze\_solvent\_polarity.py}; outputs land in \texttt{results/r2\_5\_polarity\_breakdown.\{json,csv,png\}} and \texttt{results/r2\_5\_summary.md}.
\end{changes}

\subsection*{Comment R1-16: ChemBERTa $\sim$600~nm prediction ceiling explanation}
\begin{reviewer}
ChemBERTa's parity plot shows a noticeable horizontal cluster of predictions near 600~nm, with experimental $\lambda_{\max}$ values extending well beyond that. What is the source of this prediction ceiling? Is it an artefact of input truncation, target normalisation, training-data imbalance, or some combination?
\end{reviewer}

\response{We diagnosed this with a three-hypothesis investigation reported in new Supporting Information Section~S20. The key finding is that ChemBERTa's predictions \emph{never exceed $556.4$~nm} (a hard ceiling, not gradual degradation) even though the v3 training distribution extends to $1{,}100$~nm; on the $1{,}555$ test records with experimental $\lambda_{\max} > 600$~nm, $98.5$\% are underpredicted by $\geq 50$~nm (median underprediction $= 126$~nm).

\textbf{Hypothesis testing:}
\begin{itemize}[leftmargin=2em,itemsep=2pt]
  \item \textbf{(H1) Training-target imbalance, supported.} Fold-0 train contains only $8.3$\% of records with $\lambda_{\max} > 600$~nm, $2.3$\% with $> 700$~nm, and $0.4$\% with $> 800$~nm. The deep-coloured chromophores are a long thin tail in the training distribution.
  \item \textbf{(H2) Output-range constraint, supported.} Max ChemBERTa prediction = $556$~nm regardless of input. With target normalisation (mean $= 0$, std $= 1$ during training) and MAE loss, the regression head minimises pooled MAE by regressing toward the training mean for inputs it cannot discriminate, and the maximum achievable output is bounded by the magnitudes of [CLS] activations the model has learned to produce.
  \item \textbf{(H3) SMILES-length truncation, ruled out.} Only $0.23$\% of test records exceed the MAX\_LEN $= 256$ token cap. Mean SMILES length increases from $61$ chars at $\lambda_{\max} < 400$~nm to $102$ chars at $\lambda_{\max} > 700$~nm, but the truncation rate per bin is uniformly $\leq 0.5$\%. Truncation is not the dominant cause.
\end{itemize}

\textbf{Diagnosis.} The proximate cause is the regression head's output range (H2). The deeper cause is the pretraining-representation mismatch: ChemBERTa was pretrained on ZINC (\texttt{seyonec/ChemBERTa-zinc-base-v1}), whose drug-like molecules are dominated by short-wavelength absorbers; the [CLS] embeddings ChemBERTa produces for long-wavelength chromophores (extended polyenes, push--pull cyanines, BODIPYs, porphyrins) are not well-separated in the pretrained representation from those of medium-wavelength absorbers, and the regression head fine-tuned on top of these collapsed embeddings cannot push its predictions beyond the $\sim$556~nm boundary even when shown long-wavelength training targets, because the input representations are not sufficiently distinct. Training-target imbalance (H1) compounds this. The remedy is either to pretrain on a UV-Vis-rich corpus (a different pretraining task than ZINC's drug-likeness) or to augment v3 with long-wavelength chromophores.

We have added (a) a paragraph titled ``ChemBERTa's $\sim$556~nm prediction ceiling'' to the Error Analysis subsection in the main text with the headline numbers and the three-factor diagnosis, (b) a new Limitations bullet warning practitioners not to rely on ChemBERTa's point predictions for deep-coloured chromophores, and (c) new SI Section~S20 with the full three-hypothesis investigation, a three-panel diagnostic figure (train/test distributions, parity colour-coded by SMILES truncation, SMILES length by $\lambda_{\max}$ bin), and the per-bin truncation table.}

\begin{changes}
  \item \textbf{Error Analysis subsection (pg.\ 28).} Added a new paragraph titled ``ChemBERTa's $\sim$556~nm prediction ceiling'' summarising the diagnostic and ruling out SMILES truncation as the cause. Forward pointer to SI Section~S20.
  \item \textbf{Limitations section (pg.\ 40).} Added a new bullet ``\textbf{ChemBERTa has a hard $\sim$556~nm prediction ceiling}'' giving practitioners guidance for deep-coloured chromophore use cases.
  \item \textbf{Supporting Information, new Section~S20 ``ChemBERTa $\sim$556~nm Prediction-Ceiling Diagnostic (R1-16).''} Contains the three-hypothesis investigation, the per-bin SMILES-length table (Table~\ref{tab:si_chemberta_smileslen}), the three-panel diagnostic figure (Figure~\ref{fig:si_chemberta_diagnostic}), the mechanistic explanation, and reproducibility pointer to \texttt{analyze\_chemberta\_ceiling.py}.
  \item \textbf{Code release.} Added \texttt{analyze\_chemberta\_ceiling.py}; outputs land in \texttt{results/r1\_16\_chemberta\_diagnostic.\{json,png\}}, \texttt{results/r1\_16\_chemberta\_diagnostic\_top\_y.csv}, and \texttt{results/r1\_16\_summary.md}.
\end{changes}

\clearpage

\section*{Summary of Major Changes}
\begin{itemize}[leftmargin=2em]
  \item Retitled the paper and revised the abstract and conclusion to restrict generality claims to UV absorption and closely related local optical properties; removed the ``Insights for General Molecular Property Prediction'' framing.
  \item Removed all ``RF is optimal'' language; reframed the RF/D-MPNN comparison as a statistical tie ($p = 0.77$), with RF distinguished by compute cost and operational simplicity rather than predictive accuracy.
  \item Added a new Results subsection (``Performance on Non-Local Subsets'') reporting per-model error metrics on $\lambda_{\max} > 500$~nm, $> 600$~nm, donor--acceptor, D-A-D, azo, and BODIPY subsets, with full SMARTS rules, subset counts, and a complete RMSE/MAE/$R^2$ breakdown added to the Supporting Information.
  \item Added Greenman et al.\ (\textit{Chem.\ Sci.} \textbf{2022}, \textit{13}, 1152) to Related Work and corrected the manuscript's previous overclaim that systematic solvent-encoding evaluations for UV absorption had been ``limited.''
  \item Quantified the chemical-space overlap between the primary Joung+Beard dataset and the Deep4Chem ``external'' benchmark (77.4\% of D4C solutes / 72.6\% of (solute, solvent) pairs are also in J+B) and reframed Deep4Chem as in-distribution rather than OOD validation, with the more rigorous Jung 2024 benchmark ($\sim$50\% solute overlap) carrying the OOD claim. Added a new SI section with the full overlap analysis and Venn diagram.
  \item \textbf{Adopted the Greenman/Song duplicate-handling protocol} (drop within-key groups with $\max - \min > 5$~nm, average the rest, singletons unchanged), shrinking the primary dataset from 18{,}755 to 18{,}415 records (a 1.8\% reduction). All five model families were re-trained on the v3 cleaned dataset; per-fold cross-validated metrics in Table~1, the conclusion bullets, the abstract, the SI per-fold tables, and the statistical-significance table were updated to v3 values. The headline RF~$\approx$~Chemprop tie ($p$ moves from 0.77 on v2 to 0.16 on v3) and the qualitative model ranking are preserved.
  \item \textbf{Explicitly addressed the chromophore-solvent leakage in the stratified-CV protocol} flagged by Greenman et al.\ (R1-9): added the nested-split origin of the 64/16/20 fractions to Methods, audited the v3 splits and reported the leakage magnitude (66.8\% record-level / 54.6\% unique-solute-level overlap between train and test per fold), and added a Limitations bullet framing the in-distribution Table~1 numbers as upper bounds on novel-chromophore generalisation. The chromophore-overlap effect is comparable across all five architectures ($\Delta$RMSE $+25$ to $+36$~nm between novel and seen test subsets), so the RF$\approx$Chemprop tie is also preserved on the chromophore-novel subset (paired-$t$ $p = 0.066$, $n = 5$). New SI Section~S10 reports the full per-model decomposition.
  \item \textbf{Moved the architecture math to the Supporting Information} (R1-10): the equation blocks defining RF, XGBoost, vanilla RNN, GRU gating, BiGRU concatenation, and the D-MPNN message-passing recurrence are now collected in a new SI Section~S12 (``Detailed Model Architectures''), while the main-text Methods Architecture subsections retain a 3--5 sentence summary per architecture covering citation, input representation, key hyperparameters and parameter count. The reviewer's argument was correct: these architectures are documented in detail by their original publications and the paper's core findings do not depend on the equation-level details.
  \item \textbf{Corrected ``We adapt'' to ``We use'' for Chemprop multicomponent} (R1-11): the rephrasing was absorbed implicitly by the R1-10 truncation of the D-MPNN Methods subsection.
  \item \textbf{Ran a budget-matched 25-trial Optuna probe of Chemprop hyperparameters} (R1-12) and retrained the best configuration ($d_h = 900$, depth $= 4$, dropout $= 0.1$, FFN $2 \times 800$, batch $32$; 5.6M parameters) on all five v3 folds with best-checkpoint restoration. Tuned Chemprop achieves $28.83 \pm 1.32$~nm RMSE (vs.\ default $33.15 \pm 3.27$~nm; $\Delta = -4.32$~nm, paired-$t$ $p = 0.074$, Wilcoxon $p = 0.125$); the improvement is concentrated in the elimination of the default's fold-4 outlier (39.21 $\to$ 28.46~nm). The headline RF$\approx$Chemprop tie is preserved: the tuned-vs-RF paired comparison gives $\Delta = -2.67$~nm with a parametric $p = 0.044$ but a non-parametric Wilcoxon $p = 0.125$, so we retain default Chemprop as the bolded primary in Table~\ref{tab:model_comparison} (for convention-matching with Joung/Beard/Greenman/Jung baselines), add a ``Chemprop (tuned, R1-12)'' supplementary row, and add a new SI Section~S13 documenting the search space, the per-fold tuned-vs-default and tuned-vs-RF tables, and the chromophore-novel subset numbers. The Limitations section now states explicitly that the published Chemprop defaults are undertrained for this dataset. The R1-2 response is amended to replace the previous ``variance is intrinsic to Chemprop'' clause with the HPO finding.
  \item \textbf{Added the Chemprop v2 paper to the bibliography} (R1-19): the \texttt{chempropv2\_2024} entry (Heid et al., \textit{J.\ Chem.\ Inf.\ Model.} \textbf{2024}, \textit{64}, 9--17) is now cited in the D-MPNN Methods paragraph and in the Table~\ref{tab:model_comparison} footnote justifying the choice to retain defaults as the primary Chemprop row.
 \item \textbf{Audited the wetlab benchmark's chemical-space overlap with training} (R1-13). At the SMILES level the 16-compound wetlab set is novel (15/16 absent from training; mean nearest-train Tanimoto $= 0.58$ with 5/16 below the $0.5$ ``dissimilar'' threshold), but at the Bemis--Murcko scaffold level 15/16 generic scaffolds are present in the v3 training set (only Avobenzone is scaffold-novel). We therefore reframed the Experimental Validation section's previous ``15 entirely absent from the training dataset'' phrasing as molecule-level (substitution-pattern) novelty on chromophore classes the models have seen at the scaffold level (the operational deployment scenario for photosafety and UV-filter screening) rather than scaffold-class extrapolation. A subset analysis on the five truly fingerprint-novel molecules (Tanimoto $\leq 0.5$: Amiloxate, Avobenzone, Homosalate, Octisalate, Octocrylene) shows that the DL-vs-RF MAE gap actually \emph{widens} on this stricter subset (BiGRU/Chemprop/ChemBERTa cluster at $28$--$33$~nm; RF degrades to $46.1$~nm), strengthening rather than weakening the complementary-strengths narrative. New SI Section~S14 contains the per-molecule table, the four-bin Tanimoto distribution, the strict-5 subset MAE table, and a Case-B interpretive paragraph; a Limitations bullet distinguishes molecule-level from scaffold-class generalisation; the R1-9 response footnote is amended to qualify the previous ``15 entirely absent'' phrasing. The headline complementary-strengths narrative and the abstract/conclusion bullet 1 are unchanged.
  \item \textbf{Reported the solvent-encoding ablation on the multi-solvent subset only} (R1-14). The reviewer's intuition is borne out by the data: the aggregate $6$--$8$\% RMSE reduction reported in Table~\ref{tab:solvent_ablation} is a weighted average of (a) a $21$--$32$\% reduction on the $60.3$\% of test records whose chromophore appears in the corresponding train+val slice under $\geq 2$ distinct solvents, and (b) essentially zero ($\leq 1.5$\%, slightly negative for Chemprop) on the $39.7$\% of test records on single-solvent-or-novel chromophores where the model has no solvent-specific signal to learn. The cross-architecture consistency is preserved on both subsets (RF $\geq$ Chemprop $>$ BiGRU on multi-solvent gain). We updated the abstract, the Results Solvent Ablation paragraph, and Conclusion bullet 2 with the two-number framing ``$21$--$32$\% on multi-solvent-covered chromophores and $6$--$8$\% across the full benchmark,'' and added new SI Section~S15 with the per-model decomposition table, the discussion of why the multi-solvent subset is where solvent encoding has signal to exploit, and a v2-vs-v3 footnote.
 \item \textbf{Corrected the ``optical properties'' misclassification of lipophilicity in Future Work} (R1-17). The previous Future Work sentence on cross-property benchmarking listed ``related molecular optical properties, including fluorescence emission wavelength, molar extinction coefficient ($\varepsilon$), and lipophilicity'' (lipophilicity is a partition-coefficient property and not optical. We restructured the sentence to distinguish the two property families: optical comparators (fluorescence emission, $\varepsilon$) on the one hand and globally-determined non-optical comparators (lipophilicity, aqueous solubility) on the other. The intent of the sentence) to flag cross-property benchmarking as a priority for testing the locality hypothesis, is preserved.
  \item \textbf{Updated the Data and Code Availability statement to reflect that the repository is public during peer review} (R1-18). The previous statement (``will be released on GitHub under the MIT license upon publication'') reflected the standard pre-submission availability language; the repository has in fact been publicly accessible at \url{https://github.com/Umesh1608/Paper-1_New} throughout the peer review process and has received all of the R1-1--R1-14 revision commits. We replaced the future-tense statement with a present-tense one that names the URL, lists the analysis scripts associated with this revision, and notes that trained model weights are gitignored due to size but are available on request and will be uploaded to a Zenodo archive associated with the published version.
  \item \textbf{Corrected the Mamede dataset reconstruction claim and added a full reproducibility pipeline} (R1-15). In preparing this revision we discovered that the original manuscript's claim that we ``reconstructed the Mamede/Reaxys dataset by querying Reaxys using the CAS registry numbers listed in the original study, recovering 93.6\%'' was factually incorrect: the actual reproducibility code reads Mamede et al.'s publicly available Supporting Information (a Reaxys-derived XRN+SMILES list distributed with the \emph{Scientific Reports} article) and canonicalises it through RDKit; the recovery rate is $\sim$100\% (one of 74{,}784 SMILES fails to canonicalise), not $93.6$\%. This is the second factual correction surfaced by the revision process (the first being the R1-12-driven correction to the ``variance is intrinsic to Chemprop'' claim in the R1-2 response). The revised Section~\ref{sec:classification} text reports the correct procedure, and new SI Section~S16 contains the full pipeline (SI download, RDKit canonicalisation, train/test split protocol, model configuration, output artefacts, and reproducibility command). The classification model (RF + Morgan FP, 500 estimators, 1024 bits) and its reported performance metrics are unchanged.
  \item \textbf{Added MoleculeNet contribution plan + Zenodo v3 deposit to Data and Code Availability} (R2-3). The Greenman--Song-cleaned v3 dataset is now explicitly named as a public artefact (CSV + per-fold CV indices in the GitHub repo), with a planned Zenodo deposit and MoleculeNet contribution PR for the published version.
 \item \textbf{Added pretrained-GNN and LLM/foundation-model extensions to Future Work} (R2-8, R2-9). Two new paragraphs in Future Work flag (a) graph foundation models (GROVER, MolCLR, GraphMVP, Uni-Mol, Chemprop-foundation variants) as a natural graph-side complement to ChemBERTa's pretrained-sequence baseline, and (b) general-purpose LLMs/foundation models (GPT/Claude decoders, MoLFormer, Llama/Mistral chemistry derivatives) as candidates for SMILES augmentation, data curation, and agentic workflows even when not used directly as regression heads. Five new bib entries added (\texttt{rong2020grover}, \texttt{wang2022molecular}, \texttt{liu2021pretraining}, \texttt{zhou2023unimol}, \texttt{ross2022molformer}).
  \item \textbf{Regenerated Figure~\ref{fig:parity_5models} with physically-anchored axis limits} (R2-10). Replaced the previous \texttt{min(y.min(), pred.min()) - 10} lower bound (which let outlier predictions drag the lower limit into negative wavelength territory) with \texttt{max(150, y.min() - 20)}, computed from experimental $\lambda_{\max}$ only. The new figure preserves the visual diagnostic of ChemBERTa's prediction-ceiling artefact around 600~nm while restricting the axes to the physically observed UV/Vis range.
  \item \textbf{Quantified the D-MPNN parameter-to-sample ratio and Chemprop's three regularisation mechanisms} (R2-7). Added three sentences to the D-MPNN Methods paragraph reporting the $\sim$54 parameters-per-training-example ratio (vs.\ $\sim$10--20 typical of from-scratch QSPR and $\geq 10^3$ typical of transfer learning), the three regularisation mechanisms Chemprop relies on (Noam decay, early stopping with patience 15, default dropout $= 0$), and the R1-12 HPO finding that adding modest dropout $= 0.1$ improves RMSE by $\sim$4~nm, confirming the defaults are conservative but not pathological at this dataset size.
  \item \textbf{Added a Joung+Beard dataset overview figure as new SI Figure~S1} (R2-2). Three panels: $\lambda_{\max}$ histogram, top-20 solvents by record count (log scale), and eight representative chromophore structures spanning the major class diversity. Existing SI figures S1--S4 renumbered to S2--S5; main-text Figure~S$n$ references updated accordingly. Methods Dataset Description paragraph gets a forward pointer.
  \item \textbf{Added a D-MPNN panel to Figure~\ref{fig:model_overview}} (R2-6). The architecture comparison now shows four model families instead of three, with the new D-MPNN panel illustrating the parallel solute+solvent multicomponent encoder pipeline. Layout switched from three 0.30-width minipages to four 0.23-width minipages.
  \item \textbf{Quantified the inherent experimental $\lambda_{\max}$ measurement uncertainty} (R2-1). Added a paragraph to the Methods Dataset Description section reporting the within-source duplicate spread distribution (median $= 0$~nm, $86$\% within $5$~nm, p95 $= 38$~nm) and the inter-source disagreement (n~$= 21$ Joung--Beard pairs, median $|\Delta| = 49$~nm) with the caveat about small $n$. Headroom analysis: the reported model RMSEs ($31$--$54$~nm) are comparable to the p95 of the within-source spread, so both architectures are operating near the data noise floor on a substantial fraction of test molecules. New SI Section~S17 contains the full per-source decomposition.
  \item \textbf{Added a per-chromophore solvatochromism audit} (R2-4). Of the $7{,}846$ unique chromophores in v3, $2{,}516$ appear under $\geq 2$ solvents; for these, the per-chromophore solvent-induced span has median $10$~nm, p95 $52$~nm, and a long tail to $494$~nm (Reichardt's $E_T(30)$ across $334$ solvents). The top-decile by span is dominated by classical solvatochromism standards (merocyanines, push--pull cyanines, $E_T(30)$-class betaines), exactly as the literature predicts. Added a sentence to the Solvent Information Encoding section reporting the headline numbers; full distribution histogram and top-15 table in new SI Section~S18.
  \item \textbf{Added a polarity-stratified prediction-quality breakdown} (R2-5). Per-(model, polarity-tier) RMSE on $4$ tiers (nonpolar / moderate / polar aprotic / polar protic) plus an ``other'' bucket. Headline finding: \emph{no polarity tier is dramatically worse across all models}; the relative ranking (RF $\lesssim$ Chemprop $<$ BiGRU $\ll$ ChemBERTa) is preserved on every tier. Nonpolar is easiest for all models (RF $= 27.7$~nm); ChemBERTa's degradation is concentrated on the ``other'' tier ($84.4$~nm vs.\ aggregate $54.2$~nm), the same prediction-ceiling artefact diagnosed in R1-16. SI-only addition (Section~S19); no main-text change.
  \item \textbf{Diagnosed ChemBERTa's $\sim$556~nm prediction ceiling} (R1-16). Three-hypothesis investigation: training-target imbalance and regression-head output-range constraint are supported; SMILES truncation is ruled out ($0.23$\% of test records exceed MAX\_LEN $= 256$). The deeper cause is pretraining-distribution mismatch: ChemBERTa was pretrained on ZINC (\texttt{seyonec/ChemBERTa-zinc-base-v1}), whose drug-like molecules are dominated by short-wavelength absorbers, so the [CLS] embeddings for long-wavelength dyes are not well-separated from medium-wavelength ones. Practical implication: $98.5$\% of test records with $\lambda_{\max} > 600$~nm are underpredicted by $\geq 50$~nm (median underprediction $= 126$~nm). Added a paragraph to the Error Analysis subsection in the main text, a new Limitations bullet warning practitioners not to rely on ChemBERTa for deep-coloured chromophores, and new SI Section~S20 with the full three-panel diagnostic figure.
\end{itemize}

\bigskip

We hope these revisions adequately address the reviewers' concerns. Two factual corrections discovered during the revision process (the ``intrinsic Chemprop variance'' claim in the R1-2 response, and the ``93.6\% Reaxys recovery'' claim in the Photosafety Classification section) and multiple data-driven re-analyses (chromophore leakage, solvent-encoding subset, wetlab scaffold audit, ChemBERTa prediction ceiling, Chemprop hyperparameter probe) have made the manuscript materially more accurate as well as more responsive to peer review. We thank the Associate Editor and both reviewers for the rigour and constructiveness of their feedback.

\end{document}
