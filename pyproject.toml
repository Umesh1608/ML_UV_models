[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "paper1_new_cl"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest", "ruff"]

[tool.setuptools.packages.find]
include = ["paper1_new_cl*"]
exclude = ["data*", "results*", "wetlab_uv*", "checkpoints*", "previous_code*", "papers*", "tests*"]
